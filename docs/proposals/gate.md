All key claims independently verified against source. Here is the gatekeeper disposition.

# Gatekeeper disposition — `interfaces-cors-observability` design

**Verification performed (independent of the reviews):** `platform/metrics` non-test files = 10 (cap 10, no exemption in `dirFileCountExemptions`, `directory_fanout_test.go:34,51-65`); `metrics.go` = 494 lines (cap 500, `fileSizeExemptions` empty, `maintainability_budget_test.go:34-36`); `metrics_ctor.go` = 497; `KnownEventTypes` map exists at `event_types.go:233` with `EventCIBAPingFailed: {}` at `:273`; `platform/audit` imports no `platform/metrics`; `isOriginAllowed` (`origin_validation.go:103-119`) consults only `AllowedOrigins`/wildcard; gate log at `server_login.go:174`; identity short-circuit at `cors.go:173-175`; `Metrics` struct in `metrics.go:32`; spec mandates registration in `metrics_ctor.go` (`spec:62`).

## Finding disposition (resolved / dismissed with reasons / unresolved)

| Finding (source) | Sev | Disposition | Reason |
|---|---|---|---|
| **H-1** new `platform/metrics/cors.go` breaks fan-out gate (arch/protocol/principal) | High, gate | **Unresolved** | Design still mandates the new file ("non-negotiable constraints re-verified" list checks only `metrics_ctor.go` and `interfaces/sso` ceilings, never the fan-out gate). Verified: 10→11 > 10, no exemption → `TestArchitecture_DirectoryFileFanout` fails. No dismissal given. |
| **H-2** `CORSBlockedTotal` pushes `metrics.go` 494 → >500 (arch/protocol/QA/principal) | High, gate | **Unresolved** | Design pins the field beside `CIBAPingTotal` in `metrics.go` with a 7-line Help comment (+blank, +field = +9 → 503). Field cannot leave the file; no comment trim anywhere in the design. |
| **A-1** observer signature cannot satisfy Decision 2 (arch, new) | High | **Unresolved** | `OriginBlocked(origin, method, path string, preflight bool)` carries no request/context; Decision 2 requires trace_id from context, `client_ip`, `user_agent` in the `*Server.OriginBlocked` emission. Internally contradictory as written — unimplementable verbatim. Not acknowledged. |
| **M-1/B-2** PathOverrides × login-gate telemetry 1→0 (SRE High / protocol M-1 / security F2) | Medium (deduped) | **Dismissed, unsound reason** | Design's breakage-risk 3 asserts "must not make it worse" and mitigates by *documenting out-of-scope* only. Removing `server_login.go:174`'s log while the middleware stays silent on the override-allowed edge is precisely "worse" (1 line → 0). The dismissal reason is the claim the reviewers refuted. |
| **M-2** `KnownEventTypes` entry omitted (arch, new) | Medium | **Unresolved** | Design adds the type to `event_types.go`/`aliases_spi.go` but never to the map at `event_types.go:233` (verified every SDK type is listed there). Not acknowledged. |
| **M-3/B-3** unbounded attacker-controlled metadata + sync all-paths log (security F1 / arch M-3) | Medium | **Unresolved** | No truncation cap/marker, no volume statement in `observability.md`; design only notes audit-queue drop counters (does not cover the synchronous log line with no drop path). |
| **B-4/F2** shared const ⇒ new `platform/audit → platform/metrics` import (security F3 / QA F2 / arch D2-A) | Medium (land w/ impl) | **Unresolved** | Design keeps "single source of truth via a shared string"; never addresses the import edge or the `RecordCIBAPingFailed` parameterization precedent (verified: zero cross-platform imports today). |
| **SRE F2/B-5** no alert rule/runbook for counter | Medium | **Unresolved** | Design's Help text *calls for* an alert but no `alert_rules_test.go` entry or runbook appears anywhere; not addressed. |
| **L-1** "disallowed preflight counted as a 2xx" (protocol/QA/principal) | Low | **Unresolved** | Still asserted at design line 135; reviewers verified behaviorally (404, `StdRouter` → `http.NotFound`). Factual error remains. |
| **L-2/SRE F3** `observability.md:100` diagram drift | Low/Med | **Unresolved** | Decision 3 edits exactly that line without correcting the wrong diagram (verified: code order is tracing → metrics → ratelimit → … → CORS; doc shows tracing → ratelimit → bodyLimit → metrics → CORS). |
| **QA F4** E2E registry-read vs `/metrics` scrape | Low | **Unresolved** | Principal explicitly resolved to `/metrics` scrape per `cibaPingMetricValue` precedent; design unchanged (registry reads). |
| **L-3** citation drift | Low | **Unresolved** | Stale lines remain (`control_areas.go:50-66`, `drift_test.go:41`, `metrics.go:190-196`, `server_login.go:158-178`, `middleware.go:16`, `ops_test.go:30`, `cors.go:173-176`). Immaterial to substance. |
| SRE F4/F5, QA F6-F8, security F4/F5 | Low/Info | Unresolved (doc-level) | Blocked≠refused semantics, volume baseline, E2E case-5 404 assertion, PathOverrides-blindness pin — none incorporated. |

**Result:** zero findings resolved in the design text. Two are dismissed with an explicit reason (M-1 — refuted; B-4 — implicit), the rest are unacknowledged. Three are release-blocking by any reading: H-1 and H-2 are committed-gate violations (implementation verbatim fails `make ci` — I re-derived both from the gate sources), and A-1 is an internal contradiction (Decision 2's emission payload is unreachable from the defined interface signature). The design also overrides the spec's `metrics_ctor.go` placement (spec:62) with the one placement that breaks a gate. M-1's "must not make it worse" claim is falsified by the design's own log removal.

Per the design-review gate, the design is not ready for implementation: it fails its own committed gates, cannot meet its own emission spec, and carries a refuted invariant claim. The required fixes (fold register into an existing headroom file, trim Help comment, `OriginBlocked(r *http.Request, preflight bool)`, fail-closed `WithCORS` validation or gate-side log retention, `KnownEventTypes` entry, truncation + volume doc, 404 sentence, diagram correction) are all small and exactly as pinned in the reviews — the docs-only revision is the right place for them, and it is not done.

VERDICT: FAIL - H-1 (new `platform/metrics/cors.go` breaks `TestArchitecture_DirectoryFileFanout`: 10→11 files, no exemption), H-2 (`CORSBlockedTotal` field block pushes `metrics.go` 494→503 > 500, fails `TestMaintainability_FileSizeBudget`), A-1 (`OriginBlocked(origin, method, path, preflight)` carries no request/context so Decision 2's trace_id/client_ip/user_agent emission is unimplementable), M-1/B-2 (removing `server_login.go:174` log widens PathOverrides×login-gate silence 1→0; the "must not make it worse" dismissal is refuted), M-2 (`KnownEventTypes` map entry omitted), M-3/B-3 (no truncation/volume bound for attacker-controlled metadata and the synchronous all-paths log), L-1 (false "disallowed preflight = 2xx" claim still in doc), L-2 (observability.md:100 diagram drift uncorrected where Decision 3 edits it) — none resolved in the design; all fixes are small and pinned in the reviews.

I've cross-checked every material finding against the design and source. Verified directly: `isOriginAllowed` (origin_validation.go:103-121) reads only top-level `corsPolicy.AllowedOrigins` and never `PathOverrides`; both wiring gates (`config_load.go:315`, `build_app_security.go:170`) are `Enabled && len(AllowedOrigins) > 0`; `buildConfig` (cors.go:78-90) is shared by config and SDK paths; the design's D2 row pins exactly three semantics; nested-override rejection is "推荐/次选" in the design; `shared/core/consts_wire.go:70-72` holds a second, different legacy default set.

# Gatekeeper Verdict — interfaces/cors Direction 2

## Cross-check: findings vs. design

| Finding | Status in design | Verdict |
|---|---|---|
| **High — login-gate × `path_overrides` divergence** (QA F1, protocol F1, architect A, CTO R1/C1) | **Absent.** Design's failure-mode table covers only "both empty"; D2's "empty origins disables CORS" semantic and the "无安全语义变更" claim are both false when overrides exist. Confirmed: overrides-only YAML ⇒ non-nil `corsPolicy` with empty `AllowedOrigins` ⇒ every Origin-bearing `POST /auth/login` 403s; top-level `["*"]` + narrow `/auth/login` override ⇒ gate stays permissive while operator believes it narrowed. | **UNRESOLVED — and the two blocking resolutions conflict:** CTO C1 requires a shared resolver consulted by the gate (relaxing the zero-`interfaces/sso`-changes constraint); architect D-A requires load-time rejection of origin-gated override keys (zero sso changes) and *explicitly rejects* the resolver approach as direction-3 territory. **Maintainer + security must pick one before implementation.** |
| **Medium — D2 row omits D3 merge semantics** (principal M1, CTO C2, architect C) | **Partial.** Row names all leaves incl. `allowed_headers_exclusive`, but pins only three semantics; the merge rule (defaults-first, case-insensitive dedup; `exclusive: true` restores replacement) — the direction's *only* behavior change, equally affecting the config path — is unpinned. | **UNRESOLVED — fourth pinned semantic required.** |
| **Medium — nested `path_overrides` fail-loud** (principal M2, CTO C3, architect B) | **Partial.** Design recommends rejection but explicitly retains "次选：文档声明忽略". Reviews mandate `LoadFromSources` error + negative test; the "次选" branch must be deleted. | **UNRESOLVED as written** (rejection is detectable: decoded nested map has len > 0). |
| **Medium — `enabled: false` override entries** (security F1 vs. architect E) | Design documents "仍生效"; architect dismisses security's reject-in-`validate()` as unimplementable under lenient `yaml.v3` decode (`false` ≡ unset without `*bool`/custom unmarshal — both widen the change). | **Resolved with a sound reason**; needs a recorded maintainer waiver so the security recommendation is not silently dropped. |
| **Low — equal-length prefix nondeterminism** (principal L1, QA F2, protocol F2, architect D; CTO correction) | Design's "运维可达" claim is **factually wrong** — two distinct equal-length prefixes cannot both prefix-match one path; sort + first-match is resolution-deterministic. Security F2 (operator-reachable divergence) is the outlier and is wrong. | **Resolved by correction:** tie-break is optional determinism polish only — never a correctness fix, never gated, no "undefined behavior" caveat needed. |
| **Scoped-grep ruling** (principal M3, CTO C4, architect F, security F7) | Design contains the ruling (code excl. `dist/` + five contract docs; commit-message record). Spec's own evidence line is false by its own text — also recorded. | **Resolved in design**; maintainer acknowledgment is a governance step, not a design change. |
| **Legacy `core.CORSAllowed*` second default set** (protocol F3, architect G, principal I1) | **Absent.** D3's single-sourcing claim must be scoped to the three header-name constants; legacy defaults (`consts_wire.go:70-72`, `"Content-Type, Authorization"` vs defaults' `"Authorization, Content-Type"`) recorded as non-goal + cross-ref comments. | **Unresolved** — one-line addition, Info-level, fold in at M3. |
| **Stock-path zero coverage / MaxAge / decoded-path notes / citation drift** (QA F4, architect H/I, protocol F7) | Partially addressed; architect says don't block; CTO C8 wants a cmd-side test. Citation drifts cosmetic; re-verify at M0. | Acceptable as implementation conditions (C5/C6/C8). |

## Blocking issues

1. **High — login-gate/`path_overrides` divergence is unanalyzed in the design, and the two blocking resolutions contradict each other** (CTO C1 shared-resolver-in-`interfaces/sso` vs. architect D-A load-time rejection with zero sso changes, which explicitly rejects the resolver). The design must record a single pinned decision + tests for both config shapes (overrides-only; top-level `*` + narrow `/auth/login`), with empty-effective-origins preserving today's `corsPolicy == nil` behavior. Maintainer + security decision required *before* M1.
2. **Medium — D2 doc row must pin four semantics, not three**: add the D3 merge rule (defaults-first, case-insensitive dedup) and the `allowed_headers_exclusive` escape hatch — the direction's only behavior change, operator-visible via the shared `buildConfig`.
3. **Medium — nested `path_overrides` rejection must be mandatory fail-loud at `LoadFromSources` with a negative test**; the design's "次选" documented-ignore option must be deleted.

Non-blocking but must be recorded at implementation: `enabled: false` waiver (with the architect's implementability rationale), legacy `core.CORSAllowed*` non-goal + cross-ref comments, maintainer acknowledgment of the scoped-grep ruling in the commit message, and the equal-length-prefix correction (tie-break as polish only).

VERDICT: FAIL - (1) High: login-gate/path_overrides divergence unanalyzed in the design with conflicting required resolutions (CTO C1 shared resolver vs. architect D-A load-time rejection) — maintainer+security must pick one and pin tests before M1; (2) D2 doc row pins only three semantics, must add the fourth (D3 merge rule + allowed_headers_exclusive); (3) nested path_overrides rejection must be mandatory fail-loud with negative test, "次选" branch deleted.

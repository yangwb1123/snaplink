The file already exists at `/home/dwp/snaplink/docs/RELEASE.md` with the content you shared. What would you like me to do with it? For example:

- **Update** the release process documentation
- **Validate** that the referenced Makefile targets (`make ci`, `make harness`, `make check-invariants`, `make diagnose`) actually exist
- **Check** that referenced docs (`docs/security-policy.md`, `docs/error-codes.md`, `docs/openapi.yaml`) are present
- **Something else**

Let me know how I can help!

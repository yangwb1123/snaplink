All verification complete. Here is the re-audit report.

---

# Re-audit: revised design after reviewer corrections

**Verdict: PASS — all eight corrections verified against the repository (three empirically reproduced/executed), revised design remains internally consistent with no security or wire-contract regressions. Five residual consistency edits required (none blocking).**

## A. Correction premises — all confirmed

| Correction | Premise | Verification |
|---|---|---|
| **B1** (E-1/E-5/E-6 givens) | `newLiveServer` issuer mismatch | **Empirically reproduced**: `TestSweep_GreenPath` fails `claims: iss "snaplink-sso" != discovery issuer "http://127.0.0.1:40703"` — `NewEd25519JWTIssuer` defaults to `sso.DefaultIssuer` (`ed25519_jwt_issuer.go:324`); fixture (`check_test.go:71-97`) lacks `WithEd25519Issuer(addr)`. Correct kit shape verified at `test/oidc_test.go:71-76` (shared issuer, `WithEd25519Issuer` matching `WithIssuer`). G1's AllowedScopes mechanism confirmed: empty allowlist + unwired registry → T-8d probe granted → 200 → `invalid_scope: FAIL` (`token.go:379`) → exit 1; `check INCOMPLETE` → exit 1 also verified (`check.go:92`). `Active: true` required for the mint leg. Fix is test-only. |
| **B2** (toolchain pin) | unpinned `buf generate` rewrites gen/ | **Decisive empirical test**: installed `protoc-gen-go` is v1.36.11 (committed gen says v1.34.1; go-grpc 1.5.1 + gateway v2.28.0 match); regenerating with installed tools rewrites **all 15 `.pb.go` files** (v1.36 adds `unsafe` import + state-layout change). I installed `protoc-gen-go@v1.34.1` into a scratch GOBIN and regenerated: **exactly one file differs** — `clients.pb.go`, diff scoped to fields 11/12, getters, descriptor bytes. Also: `buf` 1.59.0 sits in `~/go/bin` but is **not on PATH** (`which buf` empty; `proto-gen` target uses bare `buf`). Migration step 1 must pin `protoc-gen-go@v1.34.1` (not go.mod's v1.36.11) and make buf reachable. |
| **B3** (grep window) | `grep -A20 '^    AdminClient:'` misses block tail | Confirmed: block spans `:15971-16004` (`active` at :16004, `ListClientsResponse` at :16006); `-A20` covers only :15972-15991 — appended fields land outside → false-negative detector. **Moot now**: F3's committed check supersedes the grep entirely. |
| **F3** (committed parity check) | gap in `make ci` | **Landed and correct**: `checks/proto_openapi_parity.py` follows the `route_contract.py` pattern (module + `run()→int`, PyYAML, ROOT constants); symmetric both directions; `MESSAGE_TO_SCHEMA` pairs dict; fails loudly on oneof/nested/reserved. Registered in `cli.py` (command map + help), Makefile (`proto-openapi-parity` target **added to `make ci`**, line :265), `CHECKS_REGISTRY.md` (row :24 + command-group list :45). Tests: 8/8 pass; live: `PASS: proto/OpenAPI field parity (12 proto fields, 12 schema properties)`. |
| **R5** (`client_secret_expires_at`) | AdminClient omits proto field 9 | Confirmed at HEAD: 9 properties (fields 1-8,10), missing field 9 — a strict parity check would fail on first run without this. **Landed**: schema now carries `client_secret_expires_at` (integer/int64, mirroring `RotateSecretResponse` :16058-16063 convention), `tenant_id`, `grant_types` with read-only descriptions; parity 12/12. Doc-only, no wire impact (gateway already emits these under EmitUnpopulated). `docs-validate` and `route-contract` pass (241/348). |
| **G1** | seed `AllowedScopes`/`Active` | Folded into B1 above. One wording nit: empty AllowedScopes yields `invalid_scope: FAIL`, not a skip — conclusion (E-5 exit 1) unchanged. |
| **G2** (apiclient suite explicit) | 13 pre-existing failures | Reproduced: suite red; **all affected files are untracked** (`apiclient/{check.go,check_test.go,token.go,sweep.go,apiclient_test.go}` = `??`) — unrelated campaign content. Two classes confirmed: (a) iss mismatch (above), (b) `TestCheck_AddrValidation` raw-addr echo vs `8443` usage default. Revised plan must run `go test ./cmd/sso-ctl/apiclient/...` and report these separately (AGENTS.md §5.7); R-2's "pass unchanged" must be scoped to the change's touched tests. |
| **G3** (`-race` on test/ line) | parity with `make ci` | Reasonable; `make ci`'s race step covers the tree either way. |
| **ListExpiring** | five RPCs enumerated, mapper shared | Confirmed `admin_paginate.go:70` routes through the same `clientToProto` — R2's two-line addition covers all **six** Client-bearing RPCs (List :120, Get :212, Create :235, Update :270, Approve :358, ListExpiring :70). Revised design should enumerate six. |
| **shared/core citation** | `interfaces/sso/types.go` doesn't exist | Confirmed; corrected citation exact: `shared/core/types.go:47` (`TenantID`), `:304` (`GrantTypes`). |

## B. Internal consistency of the revised design

- **Acceptance mapping**: sound. R4 targets unmodified at `clients.go:70-84,112,146+` (181 lines); fixture-extension targets `clients_test.go:45-123` exist; `runGet` raw-map passthrough confirmed. E-1 deployment pieces all verified: `NewClientAdminService(clients, rec, nil, nil)` (:69), `sso.NewAdminMiddleware` = `admin.NewMiddleware` (`aliases.go:72`, signature :108), `stubValidator`/`adminProvider` (`test/admin_middleware_test.go:13-38`), `decodeJWTPayload` (`test/oidc_test.go:33`), gateway paths `/api/v1/admin/clients` + `/{id}` (+ `/expiring`) from the generated .gw bytes, `verifyTenantID` at `token.go:227-235` with byte-exact diagnostics (:233/:235). E-5's `mint: OK`/`check OK` match `goldenGreenStdout` (:32). T-C invariant chain verified end-to-end (`token.go:61` cc mint → `rejectDisallowedGrantType` `server_token.go:110-116`, called :56 pre-dispatch). R-1 pin passes (grpcadmin suite ok).
- **Migration steps**: with B2, step 1 = pin toolchain (protoc-gen-go@v1.34.1, go-grpc@v1.5.1, gateway@v2.28.0, buf reachable) → `cd proto && buf generate` → confirm the diff is only `clients.pb.go` → commit gen with the proto. Remaining steps order correctly; rollback symmetry intact (no storage change, overlay invariant both directions).
- **Verification plan**: F3 supersedes the B3 grep — replace it with `python cli.py check-proto-openapi-parity` (ci-gated). Add the explicit apiclient suite run + pre-existing-failure note; add `-race` to the test/ line. Current-state gates verified: `go build`/`go vet` clean, `proto-lint` ok, `docs-validate` ok, `check-test` 203/203, parity 12/12, clientscmd + grpcadmin suites ok.
- **Budgets**: all hold. `clients.go` 181 → ~191 (< 500); no new Go packages (parity check is Python, exempt from Go gates); `interfaces/sso` non-test files = 60 (at ceiling, untouched — R2 touches only `grpcadmin`); new tests are `_test.go` (exempt). **Required consistency edit**: the §7 file list must grow from 8 to ~13 (adds `gen/proto/admin/v1/clients.pb.go`, `checks/proto_openapi_parity.py`, `checks/test_proto_openapi_parity.py`, `cli.py`, `Makefile`, `CHECKS_REGISTRY.md`).

## C. Security / wire-contract regression check

None. The corrections themselves touch only test givens (B1/G1), tooling (B2/B3/F3), and documentation (R5). The production surface stays read-only: no write-mapper consumption, no routes, `Err*`, config keys, or audit events; oracle-safe table and the 8-stamp claim chain untouched. R5 closes a real drift rather than widening a contract, and the ci-gated parity check is a strictness *increase* that currently passes — no gate regression. F1/F4/F5 detectors remain valid (the tree is currently in the pre-mapper F1 state: proto edited, gen stale, but `go build` still passes because nothing references the new fields yet — the pin from B2 is what keeps the imminent regen diff scoped).

## D. Residual notes (non-blocking)

1. Wire reviewer's "buf 1.69.0" is the CI setup-*action* version; installed buf 1.59.0 suffices (proven by both generation runs).
2. Pre-existing gate failures to report separately: `TestMaintainability_FileSizeBudget` (`ed25519_jwt_issuer.go` 539 lines, unmodified) and `TestArchitecture_DirectoryDepth` (pipeline's `docs/architect-analysis/auto/runs/` artifacts) — neither touched by this change set.
3. T-8a fixture comment ("This is the real shape produced by protojson…") overstates — omits `clientSecretExpiresAt`/`loginPageUri`; fix the comment when extending the fixture.

**Bottom line**: all reviewer corrections are verified accurate and, where decisive, proven by execution; the revised design's acceptance mapping, migration steps, verification plan, and budget claims are mutually consistent once the two consistency edits above are applied (six-RPC enumeration, §7 file-list growth), with no security or wire-contract regressions introduced.

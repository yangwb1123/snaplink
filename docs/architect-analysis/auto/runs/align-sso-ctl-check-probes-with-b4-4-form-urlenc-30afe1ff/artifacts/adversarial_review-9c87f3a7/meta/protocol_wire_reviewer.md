Validation complete — **PASS on all four axes, no material defects**. Report written to `docs/architect-analysis/auto/runs/align-sso-ctl-check-probes-with-b4-4-form-urlenc-30afe1ff/artifacts/validation-0b7c9d1e/task-2-validation.md`.

## What was independently verified against HEAD

**1. Form key names vs server bind tags — exact for all five sites.** `formIntoStruct` derives form keys from the structs' `json` tags (bind.go:65-98), so: mint/T-8d keys match `oauthwire.TokenRequest` (`grant_type`/`client_id`/`client_secret`/`scope`/`Resource []string json:"resource"` — token_request.go:12-34); revoke/post-revoke keys match `revokeRequest` (handle_revoke.go:47-53) and `introspectRequest` (handle_introspect.go:75-86) (`token`/`client_id`/`client_secret`); T-9's `token` matches. Repeated `resource` keys bind via the slice branch + `formStringSlice`.

**2. Encode()/ParseForm symmetry — empirically confirmed.** Ran the exact mint values (including hostile secret `s3cr&t=+x%`) through `url.Values.Encode()` → `url.ParseQuery` (the server's `r.ParseForm()`→`r.PostForm` path): byte-perfect round-trip, sorted keys, `token=sweep-probe-dummy` for T-9 exact. Also confirmed the harness caveat is real: `ParseQuery` on a raw JSON body yields one garbage key — the design's mandatory CT dispatch is necessary.

**3. Content-Type — exact.** bind.go:28-46's only form branch is parameter-stripped `application/x-www-form-urlencoded`; the design's no-charset header is canonical and matches verbatim (its "server strips parameters anyway" = bind.go:31-34, exact). `text/plain`/absent CT both fall to `default:` → `decodeSingleJSON` — the red-today detection legs. Empirically confirmed `http.NewRequest` sends no CT header for leg B.

**4. Canonical re-encode vs real emission — confirmed for both variants.** Traced the full path: server_token.go:31-34 → `errorBody` (handlers.go:399-402) → `ErrorBodyWithTrace` (omits empty `trace_id`) → `ctx.JSON` = `json.NewEncoder.Encode` (router.go:141-148: compact, trailing `\n`, sorted keys, default HTML escaping). Executed the design's check against the actual emissions: plain passes, trace passes; extra field, whitespace, missing newline, non-string `trace_id` all fail. Key order (`error` < `trace_id`) and HTML escaping (`\u003c`-style) are identical on both sides since both use stdlib default settings.

Two non-blocking nuances: (V1) the check tolerates `trace_id:""` which the server can never emit (empty IDs omit the key); (V2) a charset-suffixed CT would also take the form branch, but the canonical header choice is strictly correct. Supporting citations (`goldenGreenStdout`, both stub discriminators, `probeClient` env-fallback-free, strict-mode 0a8df02f R3/R5/AC-1 flip dependency) re-confirmed. No corrections required.

Reconciliation complete. `design.md` updated (353 → 429 lines, documentation-only; all other worktree changes were pre-existing and untouched). Every review finding verified against HEAD first, then applied:

**1. Dedicated ACs for F1/F2 added — AC-12 and AC-13** (placed in the load-failure cluster after AC-4; numbering kept sequential since `review-operational-trust.md` references AC-1..AC-11): AC-12 locks `read checkpoint <path>: ...` (mirrors `auditexport.loadCheckpoint`'s `read anchor %s: %w`, verified at auditexport/main.go:273); AC-13 locks `parse checkpoint <path>: ...` with `{"bogus":1}`/truncated-bytes fixtures.

**2. AC-4's Given fixed**: now "signature does NOT verify under its own embedded `SignerKey` (signature bytes corrupted)", with an explicit note that "re-signed by a different key" (with `SignerKey` updated) verifies clean and is the F6 forge — redirected to AC-7b. Also documented the double enforcement (`loadAnchor` + chainer.go:446–458) as deliberate defense in depth.

**3. AC-6b exact-locked**: full-line equality `chain BROKEN: audit notary: empty chain head "", checkpoint attests "<fixture head>"` — the only fully deterministic negative line, pinning wrapper prefix and chainer propagation together.

**4. AC-8 redefined** as three NEW `Run`-level golden tests (happy exact stdout + empty stderr + exit 0; tampered `HasPrefix("chain BROKEN: audit: hash mismatch")` + exit 1 — verified that F7 errors propagate *unwrapped*, so no `audit notary:` segment; empty exact `no events to verify`). Documents that the claimed locks don't exist (`TestRun_VerifyHappyPath` is a contains-check at coverage_test.go:135; `TestVerifyDetectsTamper` never calls `Run`), states the scope (legacy `os.Exit` paths excluded), and makes AC-6a/6b the explicit anchored-branch ordering canary. Unblocked by routing all new failure paths through `Run`'s return value (`auditexport.run` precedent, `cmd/sso-ctl/main.go:72`), per the review's §5 blocker.

**5. Coverage re-verified, complete**: §5 F-table gained an AC column; §7.1 matrix maps all F1–F12 (F8→AC-3/AC-5a, F11→AC-1/AC-5b/AC-7c), with AC-8/10/11 flagged as non-failure criteria. Automated cross-check: all 12 F rows covered, every referenced AC has a row, §5 column ≡ §7.1. Also folded the review's deterministic 5-event/`--limit 4` construction into AC-9 with a reconciliation note (its predicted head-mismatch observable is exactly what the F12 fail-fast pre-empts), added the empty-chain head guard to the anchored print, byte-stability rules, and corrected the §1 drift (`checkpointAtHead` at main_test.go:503).

Review written to `docs/architect-analysis/auto/runs/wire-checkpoint-anchored-verification-into-audit-181192ed/artifacts/adversarial_review-9c87f3a7/meta/acceptance_mapping_reviewer.md`. Findings, all verified against HEAD:

**1. F→AC mapping — 3 gaps confirmed.** F1 (unreadable checkpoint) and F2 (malformed JSON) have no AC; F12 (`--limit` truncation) has no AC. Proposed AC-F1 (`read checkpoint <path>:`), AC-F2 (`parse checkpoint <path>:`), AC-F3 (5-event chain + `--limit 4` → exit 1, `does not match checkpoint attestation` — mechanism verified: `readFromFile` truncates post-reversal, so the truncated chain is internally consistent and only head equality fails). Also: AC-4's Given is wrong — "re-signed by a different key" *with `SignerKey` updated* passes embedded-key verification (that's the F6 forge); only signature-bytes corruption is F3.

**2. Wrapper confirmed, with caveats.** `VerifyChainAgainstCheckpoint` (chainer.go:469–491) emits `audit notary: ...` errors; the design's `stderr "chain BROKEN: %v"` wrapper produces `chain BROKEN: audit notary: replayed head ... does not match checkpoint attestation ...` — all substring assertions survive. But: AC-2 and AC-3 lock the two halves of the line separately, so each passes even if the other vanishes — AC-6b should exact-lock the full line (`chain BROKEN: audit notary: empty chain head "", checkpoint attests "<h>"`), the only fully deterministic negative line. And F7/F8/F11 lines are **not** byte-stable (`Recorder` stamps wall-clock timestamps, recorder.go:148), so exact assertions must be fixture-derived.

**3. Anchored stdout — found a latent panic.** The §3.1 print sketch uses `events[len(events)-1].Hash`, which panics on the AC-6a/F10 empty chain (index −1). Must guard with `head := audit.GenesisHash` (which is `""`). Locked format: `chain verified: %d event(s), head=%s, checkpoint seq=%d\n`; AC-1/5b/7c/6a switch from `contains` to full-line equality.

**4. AC-8's claimed locks don't exist.** `TestRun_VerifyHappyPath` (coverage_test.go:135, not main_test.go) uses `strings.Contains(out, "chain verified")` — no byte lock; `TestVerifyDetectsTamper` (main_test.go:159) never calls `Run` at all. AC-8 must define new Run-level golden tests locking stdout + stderr + exit codes (happy/tamper/empty), with AC-6a/6b as the explicit ordering canary (anchored branch must precede the legacy empty early return, else F9 silently fails open). **Blocker:** `usageErr`/`errorf` call `os.Exit` (main.go:227–240), so AC-4/7a/7d/F1/F2 exit codes are untestable in-process as written — route new failure paths through `Run`'s return value, per the design's own `auditexport` precedent ("never calls os.Exit", auditexport/main.go:164; `cmd/sso-ctl/main.go:54` already does `os.Exit(run(args))`, so binary behavior is unchanged).

Minor: line drifts (loadCheckpoint at 265 not 270; checkpointAtHead at 503 not 510; capture helpers live in coverage_test.go, already local).

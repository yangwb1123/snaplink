## Verification verdict: **FAIL** — the amendment pass was never applied

I verified the deliverables (`cmd-snaplink-billing-tenant-claim-design.md` + `-hardening.md`) against the three reviews, HEAD, and each other. Report written to `docs/architect-analysis/auto/runs/consume-the-b4-1-mint-time-tenant-id-claim-fail--f86cc443/artifacts/verify-amendments/verification.md`.

**1. Amendments required by the task — 11 of 13 absent, one contradicted, one applied:**

- **Protocol (all 3 unapplied)** — design:60 still says the doc comments describe "the RFC 9068 mint-time binding claims, echoed by introspection" (both the mislabel and the echo overstatement); design:64 still claims "the introspection path projects both claims with **zero extra code**" with no parity-gap statement; no IdP-side whitespace-binding sentence ("non-empty by construction" at design:63 covers only the billing-side binding). Verified against HEAD that the AS introspection echoes `serving_region` only (`introspect_body.go:52-58` vs 65-99; `types_token.go:39` has no TenantID/Roles).
- **Security (all unapplied; one contradicted)** — design:72 and the F4 row still say "payment/metering: 503"; HEAD shows payment's store error joins the existing OR → 403 `insufficient_scope` (`payment_ingest.go:157-169`), so the design contradicts both HEAD and its own §3.2.2 placement. §3.5 still targets `error-codes.md:137,1014` (137 is the SSO-ingress row where the drift check never runs; 1086, the metering row, is missing). `adminGateLog` remains per-request `Printf("...%v", err)` — and hardening:157 **contradicts** the security finding by proposing to *add* `client_id` to that line. No fail-open inversion argument, no production-observer caveat ("observe already records" — `Deps.Observer` is nil at `app.go:191-196`), no retention-path out-of-scope line, no latency note.
- **Rollout (1 applied, 1 partial, 1 not)** — T-8(a) deployment-blocking job: ✓ in hardening §2.3.1/§6 step 2. F1/F2/F5/F8 rows + §6 table: amended versions exist **only in the hardening addendum**; hardening §8 delta #1's promised parent-doc amendments were never executed (design §5 F1 still lacks TTL-bound vs unbounded; design §6 step 2 still contains the wording hardening §2.2 flags as dangerous). T-8(e) label collision: **not fixed** — hardening:89 only "recommends" renaming, and its own §6 step 4/§5.1, design §7, and requirements §7 still use T-8(e) while `implementation-gate.md:14` uses T-8(e) for IdP endpoint hardening.

**2. What did verify cleanly:** every evidence citation in both deliverables resolves against HEAD (line-exact or ±2; two nits: design §8 says quotaprojection "5 → 5 non-test files" — it is 5 total, 3 non-test; reviewer-reported mint line numbers `server_login.go:113/`native_sso.go:190` are off by 3–6 lines, and those live in the review reports, not the deliverables). The A1–A12 table resolves: all seams exist (`serveWithClaims` auth_test.go:120, `writeSourceBindings`, 4 router call sites, `payments_test.go`, metering `sourceResolverStub`, e2e fixture at `quota_projection_e2e_test.go:84-92` minting without TenantID, httptest relay seam).

**3. Internal consistency:** the F4 row and §3.2 agree with each other but are both wrong for payment and contradict hardening §2.1's correct 403 table; hardening §4.2 contradicts security finding S3b.

**4. Open-item list: not empty** — hardening §8 item 5: "The §3.1 decision (single-tenant-per-deployment vs. follow-up credential surface) is the only open item blocking §6 step 3"; the design still contains the "unimplementable sentence" that decision was to replace.

**Bottom line:** the evidence base is solid and the acceptance seams hold, but the deliverables were never amended with the review findings. The amendment pass must be redone (the report's §6 lists the exact seven edits), after which re-verification is warranted.

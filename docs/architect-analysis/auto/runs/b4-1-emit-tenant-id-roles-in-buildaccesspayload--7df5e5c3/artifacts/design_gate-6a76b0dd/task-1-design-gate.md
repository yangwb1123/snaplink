Independent verification complete. I re-verified ~40 anchors against the tree at HEAD `9cce5977` (ancestor `c676f974`, zero Go diffs — code identical to what reviewers checked).

**Resolved with verified evidence — all eleven findings + falsifiability:**
- F-1: D3 pins the gate inside `issuerForClient` (server_helpers.go:37, the single funnel); verified callers map refusal to the existing 500 `ErrNoTokenStrategy` shape (server_login.go:104 `authzErrorBodyWithState`, server_native_sso.go:184 `errorBody`); no new Err*.
- F-2: D2 excludes all five classes; I tallied the tree: all 27 cited sites match exactly (handle_par.go:170, handle_ciba.go:195, handle_introspect.go:213, handle_revoke.go:132; server_jar.go:365; 20 bearer-realm sites; server_resource.go:182; caep/receiver.go:258) and the 7 Deps `ResolveIssuer` lines exist at the cited homes; RFC 9701 (handle_introspect.go:184) flips, justified.
- F-3: D1 URL-shaped `https://snaplink.invalid/sso` (RFC 2606) satisfies both T-2 and OIDC Discovery §4.3 URL-ness; `DefaultIssuer = "snaplink-sso"` confirmed at consts_oauth.go:139; signer defaults confirmed at ed25519:259/ecdsa:267/rsa:246.
- F-4: the three tests at trusted_proxy_gate_test.go:267/283/299 assert exactly the pre-change behaviors described; harness verified with no `WithIssuer`; T1–T3 endpoint-field legs planned.
- F-5: all line budgets verified (sso.go 499, options_security.go 500, options.go 490, server_discovery.go 483, server_discovery_config.go 487, accessors.go 494, server_helpers.go 498, config_load.go 495, issue_payload.go 201, ed25519_types.go 162); 6 ordered steps with rollback rationale.
- S1–S5: DCRRequest verified without `Roles` (handle_register.go:72) + D1 wire test; ID-token `Extra: req.Claims` (ed25519_issue.go:103/ecdsa:79/rsa:70) covered by shared `scrubReservedExt`; D3 state matrix explicit; `populateIntrospectionServingRegion` echo asymmetry (introspect_body.go:51-57) pinned with test home at :716.
- Falsifiability: drift resolved (R2 = refresh-mint wire test, justified); every named test's file target verified to exist (rootcov_flow_test.go:384/:515/:191, ed25519_rfc9068_test.go:69/:230, ecdsa:60, rsa:20, rootcov_options_test.go:52, oidc_discovery_test.go:80, issuer_test.go:38, handle_introspect_test.go:688/699, testkit.go:39/128); C4 (reserved-key scrub) proven non-vacuous — `Extra: subject.Claims` unscrubbed at issue_payload.go:36 today; cross-repo gate is an explicit conjunctive X1∧X2∧X3∧K1 predicate.

**Blocking residuals found by this review:**
1. D1's blast-radius census (§1.3) claims to classify every `"snaplink-sso"` occurrence but omits `cmd/sso-ctl/configcmd/main_test.go:37` (`TestRun_InvalidConfig_Sentinel`): it substitutes the **literal** `"snaplink-sso"` as `server.issuer` and expects `config.Load` rejection (exit 1). Once the sentinel becomes a URL, that literal is a valid plain issuer → the test inverts and `go test ./...`/`make ci` fail; migration step 5 has no item for it.
2. Cross-document pin conflicts: the test plan places `WithIssuerAllowlist`/`WithIssuerNamer` in `options.go` while amendment step 4 puts them in `sso_wiring.go` (both in budget, but the two authoritative artifacts disagree), and test-plan I1(a) expects a "missing WithIssuerNamer wiring" panic message while D3 pins the "effective issuer ∉ allowlist" message — different texts in the same scenario.

VERDICT: FAIL - all eleven findings are substantively resolved with verified evidence pins, but the amendment's own blast-radius census is factually incomplete (the `cmd/sso-ctl/configcmd/main_test.go:37` literal-sentinel test inverts under D1 and breaks `make ci` with no migration item) and two companion documents disagree on load-bearing pins (options placement, construction-panic message), so the design is not yet ready as written.

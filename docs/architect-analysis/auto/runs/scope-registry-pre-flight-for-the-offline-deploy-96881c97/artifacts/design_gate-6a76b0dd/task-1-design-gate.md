I have now independently verified the blocking findings against both the design document at HEAD and the referenced code. Summary of verification:

**Verified code facts re-checked:** `server_token.go:122-152` — `rejectUnregisteredScopes` on request-borne scopes fires at :132 **before** `dispatchGrantBranch` (:152), so in a registry-wired harness the 400 for `legacy:menu:1:view` comes from the seam regardless of allowlist content. `token_client_credentials.go` — `GrantedScopes` (allowlist, incl. rule-4 default denominator) runs per-branch at :38, then post-resolution `RejectUnregistered` (reject.go:31-43). `build_stores.go:306` constructs `NewMemory(cfg.OAuth.ScopeRegistry.MatrixOrDefault(), ...ExtraScopes)` — the runtime matrix is the **provisioned** matrix when non-empty, not the built-in nine-scope `scopecontract.Matrix()`. `unmarshalClientJSON` (clients_scan.go:272) tolerates `""`/`"[]"`/`"{}"`/`"null"` and trims empty entries at runtime. `validateMappedClientBindings` (target.go:194-203) errors on any `plan.MappedClients` entry missing from `target.Clients` — before the scope gate. The design doc (mtime 16:41:58) predates the adversarial reviews (16:47-16:49) and was never updated.

**Blocking findings NOT resolved in the design:**

1. **D2 mechanism misattribution persists** — design §1 D2 and §7.3 state the clean-allowlist 400 "would come from GrantedScopes rule 1 (allowlist), masking the registry seam". False in the wired harness: the dispatch seam fires first (server_token.go:132); the dirty seed matters for *positive-leg reachability* (extra leg 200 impossible under rule 3 otherwise) and *negative-leg regression attribution* (unwired+clean → allowlist 400 stays green; unwired+dirty → 200 catches regression). Design keeps the repair but the stated rationale is wrong and unchanged.
2. **Post-resolution rule-4 default path unpinned (Material)** — all four REQ-6 legs send explicit `scope`; the empty-`scope` leg (dirty allowlist, no scope → rule-4 default → per-branch registration reject 400 at token_client_credentials.go:46) is absent; no existing test anywhere covers `token_client_credentials.go:74`-class regression. Design §7.3 has only 3 legs.
3. **REQ-5 fixtures omit `target.Clients` tenant map** — the design's unit-fixture shapes (`plan.MappedClients{"web"}` + `ClientScopes{"web": ...}` without seeding `target.Clients{"web": "tenant-acme"}`) will hit `validateMappedClientBindings`' existence error (target.go:199) before the scope gate; the asserted scope string never appears. Not addressed.
4. **S2/S3 combined single test masks per-check regressions, F12 precedence untested** — design keeps one "S2/S3 negative" test and no precedence tests; regressions in the S3 code-check or gate call-site stay green.
5. **Predicate mirror claim false for provisioned matrices** — design §2/§3.4 (E2/□1) asserts "construction-identical … can never disagree with /token", but the CLI builds from `scopecontract.Matrix()` while the server uses the config matrix when provided plus YAML `extra_scopes`; no mechanism loads server config extras, so phase-2 `--scope-registry-extra` can pass while `/token` still 400s, and a provisioned matrix can be falsely rejected. Not addressed (no `--scope-registry-config`, no fingerprint, no disclaimer).
6. **`"{}"`/empty-entry tolerance miss** — server treats `""`/`"{}"`/empty entries as empty; design's S1 parser only folds `'[]'`/`'null'` → false-positive gate errors on real rows. Not addressed.
7. **T-9 byte-compat contradicted by unconditional SELECT** — the new unconditional `COALESCE(allowed_scopes,'[]')` column means a hand-made schema lacking `allowed_scopes` now fails **even unwired**, breaking the §4.1 "byte-for-byte for every input that succeeds today" claim; needs restatement (server-created targets) or conditional read. Not addressed.
8. **F3 "empty → exit 2" unreachable; repeated-flag silent drop; empty-without-`--scope-registry` tension** — design §3.1 drops empty tokens, contradicts F3; accumulation/last-wins unsolved; rule 1's empty-extra semantics undocumented with a test. Not addressed.

The design therefore resolves none of the material findings; no explicit rejections with evidence exist anywhere in the document. Verdict:

VERDICT: FAIL - blocking findings are unresolved: D2's allowlist-masking rationale is misattributed (seam fires first at server_token.go:132; the dirty seed's real roles are positive-leg reachability and negative-leg regression attribution), the post-resolution rule-4 empty-scope path and tenant-map/REQ-5 fixtures plus S2-S3 and F12 precedence tests are missing from the test mapping, the "construction-identical" predicate claim is false for provisioned matrices configured at RuntimeOrDefault and YAML extras (no config-source/fingerprint), `"{}"`/empty-entry tolerances are not mirrored, T-9 is contradicted by unconditional SELECT, and the parse-token/flag/repeat/empty semantics stay unresoved in§3.1-F3.

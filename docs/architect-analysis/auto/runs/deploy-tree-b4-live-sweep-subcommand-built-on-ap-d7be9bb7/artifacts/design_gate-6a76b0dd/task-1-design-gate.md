All verification complete. Final assessment:

**Blocking finding (security item 1 — WithNoRedirect pin): RESOLVED.** Design §2.4 pins the additive `WithNoRedirect()` option (verified in the doc at lines 155–177); §3.3's "untouched" claim narrows to `Do/Get/Post/ReadBody/New`. I independently confirmed the api_compat premise: all 11 `apiclient.New()` call sites across the tree are zero-option (clientscmd, entitiescmd, sessionscmd, tokenscmd, tui), zero `CheckRedirect` exists in apiclient.go today, and the option loop applies only explicitly passed options — so existing subcommands' redirect behavior is structurally unchanged. Test-mapping M-7 (`TestWithNoRedirect_StopsFollowing`) covers both default-follows and stops-at-3xx, subsuming the api_compat reviewer's three required test additions; M-1/M-2 + `TestSweep_RedirectNotFollowed` cover the 3xx row semantics and the 307-body/bearer-forwarding exfiltration vectors.

**Must-fix CLI findings: RESOLVED.** F-1: no credentials → exit 2 misuse (design §5 row 25 explicitly supersedes REQ-1's skip semantics); skipped group → `check INCOMPLETE` + exit 1; `check OK` never printed on a skip-run — stated consistently across §0, §3.1, §5, §6, §7 A8 (verified at lines 23, 133, 197, 322–330, 374, 407, 438; grep found zero residual "exit unaffected" text outside supersession annotations). F-2: explicit `errors.Is(err, flag.ErrHelp) → return 0` with usage on stderr, pinned by `TestCheck_HelpExitsZero`. F-3: preflight validates the effective addr (env `SSO_ADMIN_ADDR` wins per `apiclient.New` :58–61, which I verified directly), with the exit-2-for-bad-URL deviation documented.

**Security items 2–4: RESOLVED** (userinfo/empty-host/query rejection with never-echo, R7 non-2xx-only body echo + shared `redactURL`/`sanitizeBody`, `io.ReadFull(crypto/rand.Reader)` pin with the crash-not-error correction). **Test-plan findings: RESOLVED** (A1 relocated to `dispatch_test.go` — precedent `TestSubcommands_GenerateIsWired` verified at line 9; all 25 rows + M-1..M-8 mapped).

**One non-blocking residual:** test-mapping §3 A8 (line 106), §4 row 25 (line 150), and §5 note 5 still carry the pre-F-1 criteria (`TestSkippedChecksNoCredentials` exit 0, "skip ≠ failure") behind a conditional "if the design adopts F-1" note — a conditional that has since fired. The authoritative design doc is fully consistent and names the replacement tests, so this is a self-flagged stale cross-reference in a supplementary artifact, not a design defect or a misleading exit contract.

VERDICT: PASS - All blocking findings are resolved with evidence in the authoritative design doc: the WithNoRedirect pin is verified additive and wire-compatible (zero-option call sites confirmed), F-1/F-2/F-3 exit semantics are corrected and internally consistent, security items 2-4 and all test-mapping gaps are closed with concrete tests; the only residual is a self-flagged stale note in the supplementary test-mapping doc that cannot mislead implementation.

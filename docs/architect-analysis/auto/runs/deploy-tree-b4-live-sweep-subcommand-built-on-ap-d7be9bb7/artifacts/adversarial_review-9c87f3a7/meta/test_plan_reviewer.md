Deliverable written to `docs/architect-analysis/auto/cmd-sso-ctl-apiclient-test-mapping.md`. Summary of the four asks:

**1. Testkit reuse — verified, with two line-drift corrections**
- `test/oidc_discovery_test.go`: `newDiscoveryServer` (:21–50, design says :31–50) is a real in-process `sso.NewServer` + `defaultimpl.NewMemoryClientStore` seeded with restricted client `{ID:"demo", Secret:"s", Active:true, AllowedScopes:["read","write"]}` + Ed25519 issuer + `httptest.NewServer`; 13 `TestDiscovery_*` tests, zero 404/NotFound refs. Non-empty `AllowedScopes` genuinely doubles as T-8d's restricted-client precondition (rule-2 pass-through only fires for nil/empty allowlist, scope.go:93–96). Note: the fixture is *unexported*, so reuse is by pattern (design §4.3), not literal import — a literal `import "…/test"` is layer-legal (composition→composition passes the gate) but useless today.
- `cmd/sso-ctl/dispatch_test.go` precedent confirmed: `TestSubcommands_GenerateIsWired` asserts the package-private `subcommands` map directly — which forces the design correction below.
- Budgets all confirmed gate-backed: `maxGoFilesPerDir = 10` (apiclient at 1 non-test file, no exemption → 1→3 OK), `maxSubdirsPerDir = 16` (cmd/sso-ctl at exactly 16, no exemption), `maxFileLines = 500`, `maxFuncLines = 50`, `maxFuncComplexity = 15`, all exemption caps 0. One caveat: `if`-nesting ≤3 is AGENTS.md discipline only — no committed gate.

**2. Byte-determinism — golden-testable without flakes: confirmed**, with three pins the implementation must honor (constant group-line literals on stdout; fixed slice probe order, never map iteration; all per-run variables — httptest ports, probe scope, jti/kid/iat/exp, byte dumps — stderr-only). `TestStdoutDeterministic` should assert both two-run byte-equality and a checked-in golden constant; `t.Setenv` + single-threaded orchestration rule out cross-test pollution.

**3. Mapping:** A1–A8 → tests in §3 of the artifact, with one structural fix: **A1's registration test cannot live in `apiclient/`** (design §7 header is wrong there) — it must be `TestSubcommands_CheckIsWired` in `dispatch_test.go` beside its precedent.

**4. Row coverage — honest answer: the design as written had gaps.** 12 of 25 rows were fully mapped; **rows 1, 4, 7, 9–14, 17, 18 had no failure-detection test** (rows 2, 8 partial). The artifact closes every gap with six new tests (`TestSweep_DiscoveryFetchFail`, `TestSweep_TransportErrorRow`, `TestMint_ResponseFail`, `TestClaimsMatrix_FailureDiagnostics` — one data-driven test covering rows 9–14 via the shared matrix function — `TestRevoke_Non200Fails`, `TestRevoke_StillActiveFails`) plus two data-driven extensions. After §4's additions, no failure-mode row lacks a test.

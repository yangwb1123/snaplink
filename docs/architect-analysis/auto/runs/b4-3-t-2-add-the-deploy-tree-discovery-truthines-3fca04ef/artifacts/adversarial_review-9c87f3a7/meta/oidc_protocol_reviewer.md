Validation complete. Report written to `docs/architect-analysis/auto/runs/b4-3-t-2-add-the-deploy-tree-discovery-truthines-3fca04ef/artifacts/adversarial_review-9c87f3a7/meta/discovery_semantics_reviewer.md`.

## Verdict: no blocking findings; 3 non-blocking recommendations

**Table vs emitted metadata** — All 13 JSON names are wire-correct per their specs (RFC 8414 §2, OIDC Discovery §4, RFC 7009/7662/7591/9126/8628, CIBA). All 12 non-device fields are emitted under some configuration (verified against `buildBaseMetadata` + all ten `apply*` helpers, cross-checked with route registration). **C4 confirmed**: `device_authorization_endpoint` has no struct field and zero emission paths (only `cmd/sso-minimal`'s defensive delete-list mentions it) — the "when present" guard is a harmless foreign-doc no-op. **Gaps found (F1)**: the server emits three URL surfaces the table never checks — `mtls_endpoint_aliases` (RFC 8705 §3.1, six URL strings), `mfa_endpoint` (SnapLink extension), and the three documentation URIs (`op_policy_uri`/`op_tos_uri`/`service_documentation`, shape-check-only candidates, must not be probed).

**Issuer semantics** — The exclusion-from-probing rationale is RFC 8414-sound: §3 only requires the well-known document to resolve, never the issuer URL itself, and this server 404s at its own issuer by design (no `/` route). Not asserting `issuer == base` is not just defensible but **load-bearing**: the acceptance-case-1 stock server (mirroring `newDiscoveryServer` with `WithIssuer("https://sso.test")` probed via httptest's `http://127.0.0.1`) has issuer ≠ base by construction. Gap (F5): R1.5 allows http and never rejects query/fragment — RFC 8414 §2 requires https + no query/fragment; add the query/fragment checks (free, never emitted) and document the https leniency (httptest case 1 + XFP-missing http emission).

**Probe mechanism** — Verified at code level and empirically (ran the leftover `TestZZReviewProbe*`): `StdRouter.ServeHTTP` is a linear method+path exact-segment scan, no match → `http.NotFound`; method mismatch is **404, never 405**; no catch-all exists, so non-404 on both methods structurally implies a mounted route → **no false negatives** on this router, and **no false positives** for any emitted configuration (advertised-iff-live holds by shared gating predicates). The only transient: the 5s discovery-doc cache is not invalidated by gate flips, so a ≤5s window flags a genuinely stale doc — truthful, self-correcting. Two corrections: the design's C1 status codes are wrong (empty POST → **400**, not 401/415 — independently confirming test_design_reviewer), and `test/zz_review_probe_test.go` (self-declared "deleted after review") is committed and should be removed at implement time.

No code changed; worktree preserved.

Verification complete. Report written to `docs/architect-analysis/cmd-sso-ctl-legacysync-tenant-binding-gate-security-verification.md`. All four claims confirmed against the working tree (no `.go` files touched, so no gates needed — documentation-only deliverable, consistent with the design doc's status).

## Verdicts

**1. Exact-match semantics / compensating control — CONFIRMED.** `client_gate.go:26` is a raw `r.Tenant.ID == client.TenantID` — no trimming, no normalization. The chain holds exactly as claimed: `''`-bound client short-circuits `ClientOK` to `true` at line 19 (fail-open) → grants stamp `TenantID: client.TenantID` (verified at all 8 token grants + login/native mint) → `omitempty` (`ed25519_types.go:50`) drops the claim → token **lacks `tenant_id` entirely**. The sync gate is the only compensating control. A `' '`-bound client can never match a real request tenant: host→domain→tenant resolution (`middleware.go`, sole `Set` site of `tenant:resolved`) is unreachable by a whitespace tenant id, corroborated by `tokenpolicy/validate.go:67-79` ("spellings that can never match a real tenant").

**2. M9 cannot mask — CONFIRMED, with a precise boundary.** Every auth entry point gates `clientTenantOK` → `403 tenant_mismatch` (token, login, device, MFA, federated callback, client-creds — 8 call sites). A `' '`-bound client is rejected loudly before any mint in any tenant-resolving deployment — unlike `''` it cannot silently mint, so M9 hides nothing at runtime. In the only minting regime (no resolved tenant, the server's documented fail-open for all clients), the unconditional stamp + non-empty `" "` still emits the claim exactly once (`claimsWithoutEmittedKeys` keeps the mint-time literal) — the token never *lacks* the claim; worst case is a garbage value, indistinguishable from any wrong non-empty binding, which the tool (single-DB read, no tenant store) cannot validate and the runtime surfaces immediately via 403.

**3. Oracle surface / gate position — CONFIRMED.** `main.go:52` calls `buildReport` unconditionally before the `if cfg.Apply` write branch (56-60); failure → uniform `sso-ctl legacy-sync: %v` stderr + exit 1 via `reportError` (65-67), no stdout report, both modes identical (mode only feeds `report.Mode`). Dry-run opens `mode=ro`; the gate is SELECT-only. Minor drift: write branch is 56-60, not 56-59.

**4. Mapped-client set — CONFIRMED, no exit-0 path.** `plan.MappedClients` = app-map values (`plan.go:41-46`), which is exactly the `writeAssignments` managed surface (`target.go:337`). `WHERE active=1` means `!ok` covers missing+inactive (pre-existing error, exit 1), and `''`/NULL (COALESCE) hits the new gate (exit 1). Both paths run in both modes before any write; only non-empty wrong values (M9) exit 0, by design.

Two line-number drifts noted in the report: `main.go` write branch 56-60 (design said 59), and the design's own E6 correction (gate at `server_token_clientauth.go:218-221`) re-verified as accurate.

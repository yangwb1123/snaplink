All verification complete. Summary of my independent audit:

**Resolved with evidence:**
- **F1 (HIGH, PAR RawMessage)** — Resolved in decisions doc §2 + design §3.1/F11/step 3/§3.6: RFC 9396 §3 JSON-string form encoding (reviewer's "§7.2" misnumbering corrected), fail-loud on malformed/repeated values, mandated RAR test migrates with byte-identical assertions, collateral (governance.go:61) disclosed. Sound.
- **F3 (MEDIUM, MFA params)** — Resolved in decisions doc §3 + design F12: fail-loud at `parseMFACompleteRequest` via existing `400 mfa_invalid` envelope, no audit, no new `Err*`, decoder-level rejection correctly rejected to protect the non-credential boundary. Sound.

**Still unresolved (verified independently):**
1. **F2 (HIGH)** — Design still scopes SDK emission to the 4 `tsUsesClientAuth` ops and says "JSON emission stays for all other operations". I confirmed `client.ts:2126/2136/2823/2848/2858` (`adminCompromiseCredential`, `adminReportCryptoKeyCompromise`, `postMFAComplete`, `postDeviceCode`, `postDeviceVerify`) all JSON-serialize via the generic `request()` (gen_ts_runtime.go:158-159) and target credential bind sites in the ten-site switch — all 400 post-change. The 9-op predicate + form-flag-separate-from-`clientAuth` correction is absent.
2. **F4 (MEDIUM)** — `handleDeviceVerify` (server_device.go:228-267) has no `tokenNoStoreHeaders` stamp (only :42 = handleDeviceCode); design §1 still claims "stamped before parsing at every bind site" and case 14's no-store REGRESSION-PIN would fail on `/device/verify`.
3. **F5 (MEDIUM)** — `test/introspection_jwt_test.go:70` (`postIntrospectAccept` → `/token/introspect`, used at :89/:162/:180) and `test/frontend_contract_test.go:106` (`fcPost` → `/token` at :207/:325) verified as JSON posts to credential paths; both absent from the 16-file inventory; §4 grep unchanged and evadable.
4. **Defect 1** — `TestEmit` verified nonexistent in emit_test.go (real tests at :121/:159/:180); design still names it in §3.6 case 15 and the §4 gate; also no extension of `TestTSClientAuthenticationOperations` or new form-emission assertion.
5. **Defect 2** — openapi.yaml `/backchannel-authentication` (:1584+) verified to declare both form and JSON requestBody; design step 5 still edits "seven paths"; negative-suite rows omit CIBA.
6. **Defect 3** — I ran the gate fresh: `TestArchitecture_DirectoryDepth` + `TestArchitecture_DirectorySubdirFanout` FAIL (auto/runs depth 8, fanout 89, root 24>21); design step 6/§4 still say "none known in the cited surface".
7. **Revoke-all** — no §3.6 row (prose only); **8.** `bindOAuthParams` dead-code justification still false (all 6 call sites are the credential sites being switched); **9.** count drift unfixed (measured 37+6+1 def; "44 outside the credential ten" is wrong — the true boundary is 36).

VERDICT: FAIL - F1/F3 are resolved with solid RFC-grounded decisions, but five HIGH/MEDIUM blockers remain unaddressed in the amended design: F2 (SDK emission still 4 ops, breaking postDeviceCode/postDeviceVerify/postMFAComplete and both admin-compromise ops), F4 (no /device/verify no-store stamp), F5 (sweep inventory still 16 files, missing introspection_jwt_test.go and frontend_contract_test.go), the nonexistent TestEmit gate that would pass vacuously, the missing CIBA eighth OpenAPI path/negative row, the unreported pre-existing maintainability-gate RED, plus the missing revoke-all acceptance row and the retained false bindOAuthParams dead-code justification.

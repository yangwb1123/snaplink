All verification is complete. Here is the audit report.

---

# Audit: B4-4 amended design — findings coverage and contract consistency

## Subject and method

**The amended design does not exist.** The only design artifact is `docs/architect-analysis/cmd-sso-ctl-entitiescmd-b4-4-credential-form-only-design.md` (mtime **2026-08-06 20:41:07**, untracked). All three reviews postdate it (wire_compat 20:47:57, test_acceptance 20:47:58, oauth_wire_security 20:51:32). The run directory `auto/runs/b4-4-enforce-application-x-www-form-urlencoded-o-c399c070/artifacts/` contains only `requirements-10762e10`, `design-a77de8a6` (the 31-line agent summary the deliverable was written from), and the three review metas — **no amended design, no design_gate, no later file anywhere in the tree**. I re-verified every repository fact below against the working tree, including a fresh run of the maintainability gate.

**Verdict: every finding from all three reviews is unaddressed; the deliverable on disk is the pre-review original.**

## 1. Reviewer findings coverage (F1–F6, three defects, count corrections)

| Finding | Required amendment | Status in design file |
|---|---|---|
| **F1 (HIGH)** PAR `authorization_details`/`claims` `json.RawMessage` inexpressible on form wire | Extend decoder or fail-loud + convert `TestPAR_AuthorizationDetailsSurvivesIntoAccessToken` to negative + contract note | **Missing.** No mention anywhere. Verified: `setFormField` (bind.go:113–136) handles String/Bool/Int/Pointer/[]string only — RawMessage falls through `case reflect.Slice` (Elem kind Uint8 ≠ String) and is silently dropped; the in-repo proof `handle_par_test.go:391` posts JSON with `authorization_details` and asserts token delivery. Design's case 16 and R5 case 10 give no remediation path |
| **F2 (HIGH)** SDK gap — `postDeviceCode`, `postDeviceVerify`, `postMFAComplete` still JSON | Credential-endpoint-based predicate (7 ops), not `tsUsesClientAuth` (4 ops) | **Missing.** §3.6 case 15: "postToken/postIntrospect/postRevoke/postPAR emit form bodies; **all other ops stay JSON**". Verified: TS `client.ts:2823/2848/2858` and Python `client.py:2217/2237/2245` target credential endpoints via the generic JSON `request()` |
| **F3 (MED)** MFA `params map[string]string` wire loss | Document + fail loud | **Missing.** Design F5 covers only the error envelope; `mfaCompleteRequest.Params` (server_mfa.go:230) is unformable and its loss undocumented |
| **F4 (MED)** `handleDeviceVerify` has no `tokenNoStoreHeaders` stamp | Add stamp in same change | **Missing — and the design asserts the opposite.** §1 row claims "no-store headers present — no work … stamped before parsing at every bind site: … server_device.go:42" (that is `handleDeviceCode` only). Verified: `handleDeviceVerify` (server_device.go:228–267) goes store-check → `requireDeps` → bearer auth → `bindOAuthParams` with no stamp |
| **F5 (MED)** sweep inventory 18 files, not 16; audit grep evadable | Add `introspection_jwt_test.go` + `frontend_contract_test.go`; strengthen grep to `-A2 -B2` and inspect `Header.Set` | **Missing.** "16 files" in three places (§1 row, §3.5 step 3, §3.6 R5 case 10). Verified both omitted files post JSON to credential paths: `postIntrospectAccept` (introspection_jwt_test.go:58–70, `Header.Set("Content-Type", "application/json")` at :70, used at :89/:162/:180) and `fcPost` (frontend_contract_test.go:103–106, `/token` posts at :207/:325 via path variable). §4 grep (`grep -rn '"application/json"' test/ | grep -E '/(token\|…)`) filters per-line and misses both — exactly the F5 evasions |
| **F6 (LOW)** call-site count drift (46 family sites vs 44) | Correct boundary description | **Missing.** Still "38 direct `oauth.BindParams(` + 6 `bindOAuthParams(` = 44" (§1) and "all 44 sites outside the credential ten" (§3.3). Measured: 37 non-test `oauth.BindParams(` + 6 `bindOAuthParams(` call sites + 1 definition; true non-credential boundary is **36 sites**; selfservice is 15 sites in **11 files**, not 8 |
| **Wire-compat:** "`bindOAuthParams` remains for non-credential dual-mode consumers" is false | Delete the delegate / note removal | **Missing.** §3.1 keeps the false justification. Verified all six `bindOAuthParams(` callers are credential (server_token.go:30, server_device.go:53/242, server_mfa.go:255, server_admin_handlers.go:293, options_admin.go:414) — the delegate becomes dead code at step 2 |
| **Defect 1:** `TestEmit` does not exist (silently no-ops the SDK gate) | Name `TestTSClientAuthenticationOperations` + `TestGenerateTS_BalancedBracesAndNoRawTemplateLeftovers`; `-run 'TestTSClientAuthentication|TestGenerateTS'` | **Missing.** Case 15 and §4 still name `TestEmit`. Verified: emit_test.go has no such func (real tests at :159, :121, :148, :180); `go test -run TestEmit` exits 0 with no tests |
| **Defect 2:** CIBA unnamed in negatives + missing from OpenAPI edit | JSON-rejection row for `/backchannel-authentication`; eighth OpenAPI path | **Missing.** `JSONRejected` row lists token/introspect/revoke/par/device-code/device-verify + mfa + admin only; step 5 edits "seven paths". Requirements mandate CIBA (req :71, :114 "any of the ten endpoints") |
| **Defect 3:** pre-existing maintainability gate failure unreported | Report in step 6 per AGENTS.md §5.7 | **Missing.** Step 6: "none known in the cited surface". Fresh run confirms RED at HEAD: `TestArchitecture_DirectoryDepth` (auto/runs to depth 8) + `TestArchitecture_DirectorySubdirFanout` (docs 18>16, auto/runs 89>16, root 24>frozen 21) |
| Revoke-all exclusion lives only in prose | Explicit `REGRESSION-PIN`/`UNCHANGED` row in §3.6 | **Missing.** §3.6 has no revoke-all row; exclusion is §2 prose only (verified correct in code: `HandleRevokeAll` at handle_revoke.go:181 binds no body) |

## 2. The six mandated verifications

1. **Eight OpenAPI credential paths** — **FAIL**. The design lists seven everywhere (§1 :24, §3.3 :62, §3.4 F7 :75, step 5 :89) and never names `/backchannel-authentication`. Verified in `docs/openapi.yaml`: path at **:1584**, requestBody :1599–1608 declares `application/x-www-form-urlencoded` (:1604) **and `application/json` (:1607)** — the user-cited :1584–1607 range is exact. CIBA is also the 10th R1 endpoint (handle_ciba.go:87) with a mandated `400 invalid_request` (requirements :71), so the omission is a contract defect, not a scope call.
2. **docs/error-codes.md + CHANGELOG breaking-wire entries** — **Present** (step 5: "`docs/error-codes.md`: note credential endpoints accept form only. CHANGELOG: breaking wire-change entry"). No new `Err*` is correct (sentinel stays unexported). These two survive the original, but the OpenAPI bullet they sit beside is the seven-path error.
3. **`tokenNoStoreHeaders` stamp for `handleDeviceVerify` (F4)** — **FAIL**, absent; the design's §1 row affirmatively claims the opposite ("already present — no work").
4. **18-file sweep inventory + strengthened `-A2/-B2` audit grep** — **FAIL**. Still 16 files; both `introspection_jwt_test.go` and `frontend_contract_test.go` omitted; §4 grep unchanged and demonstrably evadable by both omitted files.
5. **Revoke-all `REGRESSION-PIN` row** — **FAIL**, absent from §3.6 (only the T-8(b)1-4, T-8(c)5-6, T-8(e)7-8, R3 9/13/14, R5 10/15, T-9 11/12, case-16 rows exist).
6. **Pre-existing gate failure in step 6** — **FAIL**. Step 6: "none known in the cited surface; `checks/directory_fanout.py` state unchanged". Fresh `go test -run 'TestMaintainability_|TestArchitecture_' .` = **FAIL** (depth + fanout, all from the tracked `docs/architect-analysis/auto/runs` artifact tree — the B4-4 change itself never touches it, exactly why §5.7 reporting matters).

## 3. What is sound and unchanged

The mechanism the reviews approved is not in dispute: sentinel design (oracle-safe, no `errors.Is` at any of the ten sites), byte-identical `bind.go` refactor, ten-site switch list (complete — includes options_admin.go:414 and handle_ciba.go:87; revoke-all correctly excluded), `/auth/login` and CLI exclusions, `interfaces/sso` staying at 60 files, oauthwire 6→7. The 400(JSON)/401(form) split is wire-format-determined, never state-determined. None of this needs rework — the defects are in the mapping/coverage/contract-update layer, and every one of them is still unfixed.

## VERDICT: FAIL - the amended design was never produced: the on-disk deliverable (mtime 2026-08-06 20:41, predating all three reviews) addresses none of F1-F6, none of the three acceptance defects, none of the wire-compat count corrections, and fails all six mandated verifications (eight OpenAPI paths, handleDeviceVerify no-store stamp, 18-file sweep with -A2/-B2 grep, revoke-all REGRESSION-PIN row, step-6 pre-existing gate report); only the docs/error-codes.md and CHANGELOG bullets survive from the original; the design must be amended with all of the above before the design_gate stage can pass.

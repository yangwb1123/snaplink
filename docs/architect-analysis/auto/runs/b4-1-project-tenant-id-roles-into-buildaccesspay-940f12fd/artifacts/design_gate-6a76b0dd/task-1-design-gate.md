All verification complete. Here is my independent audit.

## Verification report

**Baseline state:** the design doc (`cmd-sso-ctl-entitiescmd-b4-1-tenant-claims-design.md`, mtime 20:17) predates all three review artifacts (20:21–20:25), and the run `b4-1-project-tenant-id-roles-into-buildaccesspay-940f12fd` contains no design revision, review-response, or resolution artifact — the adversarial_review stage is the last stage. The reviewers' closing offers to patch the doc were never acted on. I re-checked each contested claim myself:

**Security reviewer Q2 (material) — unresolved, claim still false.** Doc §3.1 item 2 and F3 row still state "`omitempty` omits only nil slices, not empty non-nil ones" / "omitempty does not drop empty slices". My empirical Go check: `omitempty` drops both nil and empty non-nil slices (`nil → {}`, `empty → {}`, `["a"] → {"roles":["a"]}`). The R0 contract pin — which the sibling R0 module implements from — still carries a factually wrong mechanism rationale; the reviewer required this correction "before this document serves as the R0 contract pin". Also unresolved: F2 names no audit event ("detail only in audit", no event identifier anywhere), and the in-test JWT-bearer validator is not pinned to collapse failures to one opaque error.

**Test-design reviewer F-1/F-2/F-3/F-4 — unresolved, classifications still wrong.** (F-1) Cases 2/7 list `aud` in the green set; I verified `aud` is emitted only when `len(subject.Resources) > 0` (issue_payload.go:116-120) and the doc has zero `resource`/`Resources` mentions — the assertions fail at HEAD for a fixture reason. (F-2) Cases 3/11 need `WithEd25519Issuer("https://sso.test")` pinned: `NewEd25519JWTIssuer` defaults to `DefaultIssuer` = "snaplink-sso" (ed25519_jwt_issuer.go:259), and `sso.WithIssuer` feeds only discovery config (server_discovery_config.go:260-265), never the minted `iss`; the doc's §3.2 fixture and the case-3 hedge are as-written wrong. (F-3) Case 9's "RED-pre-R0 (both halves)" — the negative half is GREEN-always. (F-4) Case 10 "RED-pre-R0" — no mint-time lookup exists pre-R0, so it passes vacuously; same flaw in failure-mode F2.

**Engineering reviewer C1–C3 — unresolved.** (C3, material) Doc §3.5/§4 claim "fan-out must stay green" / "the committed Go gate passes"; I ran the gate: `TestArchitecture_DirectorySubdirFanout` FAIL (root = 24 subdirs > frozen 21) and `TestArchitecture_DirectoryDepth` FAIL at HEAD — pre-existing red, so the doc's baseline assertion is inaccurate. (C2) `entitiescmd` is 2 non-test files (tenants.go, users.go), not "4 non-test files" as stated in §1 and §3.3. (C1) No explicit per-file line budgets stated.

**Confirmed-correct items (no change needed):** the 8 grant-path `TenantID` stamps, `Subject.TenantID` "NOT a token claim" doc, `resolveIssuer` Host fallback, the JWT-bearer seam correction (§2), 16/16 `cmd/sso-ctl` fan-out and 60-file `interfaces/sso` ceilings, and T-9 byte-identity scope.

**Conclusion:** every substantive review finding (Q2 mechanism error, F2 audit-event name, test-validator opaque-error pin, F-1 through F-4, C2/C3) remains open, verbatim, in the deliverable — none resolved, none explicitly rejected with evidence anywhere in the run. The design cannot serve as the R0 contract pin in its current state.

VERDICT: FAIL - All blocking review findings (omitempty mechanism error in the R0 pin, aud/issuer fixture pins F-1/F-2, mis-stated RED flags F-3/F-4, audit-event naming, opaque-error pin, wrong entitiescmd count, and the already-red TestArchitecture_ claim) remain unresolved and un-rejected; the design doc is byte-unchanged since before the reviews, with no resolution artifact or revision in the run.

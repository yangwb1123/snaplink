Analysis complete. Full deliverable: `docs/architect-analysis/auto/runs/replace-host-derived-issuer-fallback-with-a-conf-ed324fea/artifacts/strict-mode-availability-abuse-analysis/task-2-analysis.md` — every claim re-verified at HEAD (`234fdd7b`) before writing.

## Decisions (with file:line homes)

**D1 — Disallowed-issuer log flood** (attacker-spoofed Host/XFH at untrusted edges; `ForwardedHeadersTrusted` defaults true at `shared/security/peertrust/request.go:37-39`; `spi.Logger` has no Warn — `shared/spi/logger.go:6-10`):
- Per-request detail at **Debug** (default production level `info` drops it → zero default flood); one **throttled Error per 10s, single-slot, server-wide** (no per-issuer map — attacker-controlled cardinality would be a memory DoS), carrying a suppressed-count; **no audit event** (spoofable input into an async bounded sink = second flood vector).
- Homes: option → `options_passwd.go` (464; `options.go` is 499, `sso_protocol.go` 500 — rejected by budget, per audit-7d1f9c02); field+throttle → `sso_wiring.go` (462); strict branch → `server_discovery.go:251-256`; accessor → `server_discovery_cache.go` (343).

**D2 — `/health` raw `s.issuer`**: **explicit empty, never request-derived**. `handleHealth` (`handlers.go:411-425`) switches to the shared `configuredIssuer()` helper: mode-off byte-identical; strict+allowlisted → configured issuer; strict+sentinel → `"issuer":""` (key always present). Rejected masking (wire-shape change) and deferral (sentinel leak). RFC 9728 companion: emit `[""]` (`server_resource.go:144,180-182`, pin-only; mirrors discovery's non-omitempty visible `""`).

**D3 — Discovery cache coherence**: allowlist becomes a safe-reload path (`config/reload/reload.go:85-98,276-329` + `wireIssuerAllowlistReload` in `main_wiring.go:134-166` region) with **swap-then-flush** (`invalidateDiscoveryCaches`, `server_discovery_cache.go:270-283`); reload failure keeps last-good (no partial state, no 503). Main signing-key rotation is coherent by construction (discovery carries only URLs/alg names); `KindSigningKeyRotation` arm (`server_invalidation.go:322-324`) gains a discoveryDocCache sweep for `signed_metadata` (same issuer signs both). `server.issuer` stays boot-only. TTL (5s) is backstop, not correctness; epoch cache keys rejected.

**D4 — Rotating-Host test design**: 21-entry boot-path sweep (§4.1 table, per-entry handler homes) × {raw-Host, XFH-spoof} × {strict, mode-off}; per-response invariant `iss ∈ {configured, ""}` — never host-derived, even mid-swap; status == mode-off baseline (no new 5xx/503); `t.Parallel()` + `-race` + `-count=10`; swapper goroutine drives `SetAllowedIssuers`+flush (reload simulation); new files `interfaces/sso/issuer_allowlist_race_test.go`, `cmd/sso-server/issuer_allowlist_reload_test.go` (real Reloader, last-good leg), `config/issuer_allowlist_test.go` (5-YAML migration). **Blocking precondition**: B1 reject-on-empty guard fixes at `server_jar.go:246-248` and `server_pairwise.go:343` — the only genuinely new attack surface strict mode activates.

**Pre-existing gate failure (reported separately, verified)**: `TestMaintainability_FileSizeBudget` fails at HEAD — `infrastructure/defaultimpl/ed25519_jwt_issuer.go` is 539 lines; must be split before `make ci`.

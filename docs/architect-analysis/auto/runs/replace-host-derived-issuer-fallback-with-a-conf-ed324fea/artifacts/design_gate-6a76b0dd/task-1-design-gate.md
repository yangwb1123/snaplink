All evidence independently verified. Summary of what I confirmed at HEAD (`234fdd7b` + working tree):

**1. The design deliverable is unchanged.** `design-a77de8a6/task-1-design.md` is 2255 bytes — the same 15-line summary. It contains zero occurrences of B1/B2/B3/B4, reject-on-empty, empty-allowlist semantics, a `NewServer` decision, or the corrected file homes. The full design promised by the summary ("Failure modes F1–F7… acceptance mapped to A-1..A-7 with concrete test files") was never written; the closure reviewer's "NOT CLOSED in the design document" verdict holds.

**2. Blocking placement claims disproven at HEAD (verified line counts):** `options.go` = 499, `sso_protocol.go` = 500 (already at the frozen `maxFileLines=500`, exemptions empty per `maintainability_budget_test.go:34-36`), `config_load.go` = 497 (needs +4 → 501). The design's stated homes (`WithAllowedIssuers` in options.go, field in sso_protocol.go) each cross the budget; the verified-safe homes (`options_passwd.go` 464, `sso_wiring.go` 462, sentinel-block fold in `config_load.go:183-186`) appear only in reviewer artifacts.

**3. B1 confirmed real and unresolved in the design:** `server_jar.go:248` (`len(p.Aud) > 0 && asIssuer != "" && …`) and `server_pairwise.go:343` (`asIssuer != "" && …`) both skip aud binding on empty issuer; the design's `""` sentinel activates them with no reject-on-empty branch specified. **B2 confirmed:** the only discovery override (`server_discovery_config.go:264-265`) is sentinel-conditional, so the design's one-line "discovery normalized" claim lacks the required unconditional mechanism.

**4. Pre-existing gate failure confirmed (reported separately, not induced):** `infrastructure/defaultimpl/ed25519_jwt_issuer.go` = 539 lines > 500 — `TestMaintainability_FileSizeBudget` fails at committed HEAD regardless of this design.

The B1–B4 resolutions, D1–D4 decisions, and corrected placement exist only in the downstream reviewer artifacts (`wire-hardening-84081c98`, `closure-verification`, `strict-mode-availability-abuse-analysis`) — those documents themselves state the revised design "was never written" and that the design must state these items. Resolutions living in review artifacts do not satisfy a design gate on the design deliverable; nothing was explicitly rejected with evidence either.

VERDICT: FAIL - the design document still contains no resolution of the blocking findings (B1 reject-on-empty, B2 unconditional discovery normalization, B3 exact-match membership, B4 mode-gate semantics, empty-allowlist definition, T-2.3 decision) and its stated file homes (options.go 499, sso_protocol.go 500) violate the frozen 500-line budget, with the revised design never written; the resolutions exist only in downstream reviewer artifacts.

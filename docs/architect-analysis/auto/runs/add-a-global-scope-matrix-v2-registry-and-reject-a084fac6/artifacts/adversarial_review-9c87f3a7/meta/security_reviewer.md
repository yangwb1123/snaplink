Oracle-safety pass complete. Report: `docs/architect-analysis/auto/runs/add-a-global-scope-matrix-v2-registry-and-reject-a084fac6/artifacts/oracle-safety-pass/task-1-oracle-safety-pass.md`. All verification was against the worktree code plus throwaway probes (written, run, removed — `git status` shows no residue).

## Findings

**1. Byte-identity — VERIFIED.** `RejectUnregistered` (shared by the seam at `server_token.go:132` and all 9 per-branch sites) emits exactly `{"error":"invalid_scope"}` via `core.ErrorBody` — no trace_id, no description, no scope name. Status/headers are identical too: `handleToken` stamps no-store before any branch. `denyTokenScopeCombo` collapses to the same 400/`invalid_scope`; its trace-wrapped body (only when `WithTracingMiddleware` is installed) is a pre-existing class distinction, not a new observable. Pinned by `TestScopeRegistry_UnregisteredRejected_ByteIdenticalBody` + `TestRejectUnregistered`; probe re-confirmed live.

**2. Timing — QUALIFIED.** `Memory.Registered` is a linear scan, not constant-time: measured 3.9 ns (early exact hit) vs 31 ns (unregistered full scan). ~27 ns absolute delta — network-unobservable, and for single-scope probes the 400-vs-200 outcome already reveals registration (the designed signal). No new oracle class; recorded as a qualification with the cheap hardening option (map-backed exact match) if a constant-time bar is ever imposed.

**3. Metric exposure — VERIFIED BY ABSENCE.** No metric exists: the reject path calls only `ctx.JSON`; `docs/observability.md` explicitly promises "no new audit event type and no new metric". The migration reviewer's recommended rejection counter was not implemented, so there is no exposure channel. Caveat recorded: if that counter is added later it must stay operator-facing and bound cardinality.

**4. Config-ahead-of-code — VERIFIED WITH NUANCE.** Simulated a pre-feature struct against `decodeStrictWithFallback`: the unknown `scope_registry` block is dropped **atomically** (both `enabled` and `extra_scopes` vanish; sibling keys survive; WARN logged). The old binary is exactly `enabled:false` — no partial gate possible, so fail-closed holds; the residual is the operational "flip looks taken but isn't", detectable via the boot log line on new binaries vs the unknown-keys warn on old ones. Always-on grammar validation (fails boot loudly even with `enabled:false`) is pinned and probe-verified.

**5. Restart-while-enabled mixed registry — VERIFIED, with one correction to the migration reviewer.** Probe ran replicas A (extra scope) and B (base matrix): A mints, B rejects with the standard plain body, never mints. But the reviewer's Q1/Q2 refresh claim is **wrong as stated**: `store.Consume` (:116) precedes the registry check (:167) — only the *rotation* is skipped. Empirically: first presentation → 400 `invalid_scope` (leaf deleted); retry of the same token → 400 `invalid_grant` via `ErrRefreshTokenReused`, which **kills the whole family** on all four shipped backends. So "the same token works post-rollback" is false, and a natural client retry escalates a wrong-registry canary to forced re-login. The report lists three remediation options in order of leverage (move the refresh check before Consume; tombstone-without-reuse-arming; or restate the plan with the kill risk explicit).

Gates: build/vet clean; `TestScopeRegistry` 8/8 + race clean; package suites pass. The pre-existing `TestArchitecture_DirectoryDepth`/`DirectorySubdirFanout` failures re-confirmed as docs-tree-only (138 mentions, all `docs/architect-analysis/auto/**`), unchanged by this pass.

Analysis delivered to `docs/architect-analysis/auto/runs/add-a-global-scope-matrix-v2-registry-and-reject-a084fac6/artifacts/migration-stress-test/task-1-migration-stress-test.md`. Every claim was verified against the worktree code. Summary:

**Q1 — Mixed-version fleet behavior.** All single-use artifacts are **consumed before the registry check** (authcode `token_authcode.go:183→88`, device `:65→92`, CIBA `:61→116`), so during any mixed window a rejection is terminal for the artifact — a canary/ON hit burns the user's flow; the same code would mint at an OFF replica. **Refresh is the only long-lived class and is correctly NOT consumed** (`token_refresh.go:167` precedes rotate), making rejections retryable — that's what makes the plan survivable. Registry rejections consume no rate-limit budget (seam `:132` precedes `:148`) and never poison the idempotency cache (2xx-only caching). Old binaries **silently skip the gate** — `decodeStrictWithFallback` (`config/source.go:260`) ignores the unknown key with a warn, so a config-ahead-of-code flip degrades to a partial gate with no loud failure.

**Q2 — Rollback-by-flip: restores the decision function, not the artifacts.** New attempts post-rollback are byte-for-byte identical (nil registry = structural no-op, pinned by `TestScopeRegistry_DefaultPathUnwired_ByteIdentical`). Residual denies: (D-1) burned auth codes/device codes/CIBA requests stay consumed (TTL-bounded, same class as any failed exchange today); (D-2) families rotated while enabled stay permanently narrowed; (D-3) discovery lags ≤5s per pod (under-advertise only). Rejected refresh families are NOT a residual — the same token works post-rollback, and nothing downstream (introspection/revocation) consults the registry.

**Q3 — Drain × frozen registry.** The "drain" is mislabeled: nothing drains — families are rejected-not-consumed and sit permanently until rollback or a registry fix, with **no completion signal** (rejections are byte-identical to ordinary `invalid_scope`, no trace_id, no audit event, no log — pinned by A-8f). The only remediation for a wrong registry is a restart-while-enabled, recreating the mixed window with two registry *contents*. Canary must be config-final: a canary-only `extra_scope` gets minted by the canary and rejected by the post-flip fleet (self-inflicted drain).

**Q4 — Provisioning races.** R-1 (flip before scopes complete): recoverable for refresh, terminal for codes. R-2 (well-formed typo): **undetectable** — grammar validation is always-on but completeness is unverifiable pre-flip. R-3 (config before code): silent partial gate. R-5 (amending `extra_scopes` while enabled): fleet-level discovery over-advertise relative to old pods. All races are safe-by-construction against idempotency poisoning and rate-limit amplification.

Top recommendations: (1) a server-side per-scope rejection counter (metric, not a response surface — no oracle leak, no wire change) to make drain/rollback verifiable — the one change that unblocks the plan; (2) pin sequencing (code-before-flip, never amend `extra_scopes` while enabled, canary config == final config); (3) restate "drain" as "soak/observe" and "rollback" as restoring the gate for new attempts only.

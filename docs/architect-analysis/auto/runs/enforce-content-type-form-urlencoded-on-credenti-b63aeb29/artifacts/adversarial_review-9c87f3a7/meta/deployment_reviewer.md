All pins green. Reconciliation complete — here is the report.

## Ground truth (verified at working tree, HEAD `880443ef`)

| Claim | Measured reality |
|---|---|
| Flip already true | `ops/deploy/compose/config.yaml:21` `require_form_content_type: true`, with a comment declaring the single-flip-point contract; `test/deploy_form_only_sweep_test.go` `TestDeployTreeRequireFormContentTypeSingleFlipPoint` pins (a) true-exactly-once in compose, (b) literal absent from every other file under `ops/deploy/`, (c) documented in `docs/config-reference.md` |
| Landed semantics | **Opt-in, default-off** — not the named doc's "default true": `sso.go:76` seeds `credentialFormOnly = false` ("explicit seed guards a future default flip"); `config_server.go:214-223` `credentialFormOnlyOptions()` appends nothing when `nil`; config comment `config_server.go:61-71`: unset/false = byte-identical legacy, "Boot-time only; no hot-reload; rollback = drop the key or set false" |
| Wired surface | Four endpoints only: `server_token.go:30`, `handle_introspect.go:154`, `handle_revoke.go:82`, `handle_par.go:73` → `BindParamsFormOnly` (`bind_strict.go:39-45`, `ErrFormOnly` sentinel, reject-before-body-read); CIBA/device/MFA dual-mode (out of scope). Contract is **415** plain `{"error":"invalid_request"}` (`openapi.yaml:1069-1075`, `error-codes.md:332`) — the requirements' literal "400" was superseded, and the stub correctly says 415 |
| R7.1 gap | Confirmed still open: `assertRetentionTokenRequest` (`quota_relay_test.go:94-106`) and `assertQuotaTokenRequest` (`:149-162`) assert BasicAuth + `PostForm` body only, never the CT header |
| R7.2 test | Does **not** exist yet (`test/` has `audit_provisioner_form_e2e_test.go` — the sibling — but no billing e2e); `NewOAuthTokenSource`/`NewPlatformTokenSource` grep in `test/` hits only the sibling. It is the unimplemented delta |
| readyz-503 chain | `app.go:93-104` wires `audit_relay_module`/`tenant_quota_projection` → `auditModule.Ready` (`relay.go:295`) / `quotaRelayRunner.Ready` (`quota_relay.go:472`); token-fetch failure → `ErrTokenUnavailable` (`quota_relay.go:407`); `readyz` → 503 on any failed check (`health.go:44-56`); `health_test.go:31` pins the 503 shape |
| Tests | `TestDeployTreeRequireFormContentTypeSingleFlipPoint`, `TestFormEncoded_JSONStillWorks` (still the **positive** JSON→200 case), `TestCredentialContentType`, and the billing relay tests all green just now |

## Per-item verdicts

- **R7.2 — accurate, describes the shipped mode.** It pins exactly the shipped posture (strict server via explicit `WithCredentialFormOnly(true)` — robust to the future default flip — plus the real `NewOAuthTokenSource`/`NewPlatformTokenSource` mints that are precisely the compose consumers). It is not hypothetical hardening; it is the *missing* pin for the deployment mode compose already runs. Caveat: it does not exist yet — it *is* the delta.
- **Flip boot-time key steps — accurate as a runbook, but the flip is already executed for compose.** "Verify consumers → flip boot-time key → observe readyz → rollback by key removal" correctly describes the shipped mode's lifecycle, but the "flip" step is a *completed* step in the compose tree (line 21, sweep-pinned) and a live step only for helm/baremetal/k8s, which the sweep test deliberately keeps off. The stub's own compatibility line ("compose already strict with a single pinned flip point") is consistent; the migration bullet should carry the qualifier so it cannot be read as an upcoming flip.
- **readyz-503 symptom — accurate, shipped-mode symptom.** Unmigrated JSON/missing-CT consumer → 415 → `ErrTokenUnavailable` → relay Ready fails → `/readyz` 503. This is the observable failure of the mode already running in compose, and the design's framing (F7, T-8(e)) matches the code.
- **Rollback-by-key-removal — accurate.** Drop the key → `nil` → option not appended → `credentialFormOnly=false` → byte-identical legacy; boot-time only, no hot-reload. Matches the landed comment and `credentialFormOnlyOptions()`.

## Flagged steps that still assume the key is off (or pre-flip state)

1. **The stub's "flip boot-time key" operator step** — the one step that assumes an off key. For the shipped compose deployment it is a no-op (already true); keep it only as the runbook for the unflipped trees. Recommend rewording the migration bullet: "compose already ships strict; the runbook (verify consumers → flip key → observe readyz → rollback by key removal) applies to helm/baremetal/k8s adoption."
2. **The 396-line named design doc** (`docs/architect-analysis/cmd-snaplink-billing-b4-4-credential-form-only-design.md`, written at pre-landing HEAD `30938444`) is superseded and must not be re-executed: §3.3 "Breaking by design (default true)" never shipped; §3.5 step 4 ("invert `TestFormEncoded_JSONStillWorks` to the negative case") and step 5 ("sweep every in-repo JSON post to the eight endpoints") describe the strict-default world that did not ship — the landed tree keeps JSON working on the default harness (verified green) and wires four endpoints, not eight. Its steps 1–3/7–8 (binder core, plumbing, site switches, config/docs) are already landed; step 8's gensdk form-emission is likewise done (credential SDK tests exist).
3. **Minor labeling drift:** the stub's "7-step verification plan" matches no enumerated plan — the requirements doc §10 has **six** steps (ending at `make ci`) and the named design §4 has ten commands. The substance (targeted strict tests → `-race` → E2E → `make ci`) is intact; the count should be corrected to six.

One delivery caveat beyond the design: the entire strict surface — `bind_strict.go`, the config/option edits, and the compose flip itself — is **uncommitted working-tree state** as of HEAD `880443ef` (the design-stage commit only adds the stub). "Shipped" holds in the landed-and-gate-covered sense the pipeline verified against `0aab82b5`; the flip's provenance is the in-flight implement work from run `0a23f017`, not a committed deploy history entry.

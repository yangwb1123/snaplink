Review complete — report at `docs/architect-analysis/auto/runs/enforce-content-type-form-urlencoded-on-credenti-b63aeb29/artifacts/engineering-gates-review.md`. Everything was re-verified against the live tree, not just the cited evidence.

## Verdicts

**1. Placement in `test/` — CONFIRMED.** All 214 files in `test/` are `package ssotest`; DIRECTORY_MAP classifies `test/` as composition/tooling. The sibling is the exact precedent (same three imports, gofmt-clean, race-clean). `test/` has no `go.mod` (root module), so `make race`/`make test-e2e` (`go test -race -count=1 ./test/...`) cover it. Feasibility of the three mints confirmed at `relay.go:45-52`, `quota_relay.go:347-352, 420-428` — all exported `auditgovernance` API. One implementation detail: the quota source needs the tenant-scoped `SourcePrefix` (`snaplink-billing-quota:tenant-a`), whose prefix const is unexported — the e2e must hardcode the literal.

**2. FileSizeBudget — CONFIRMED, but the design's claim is unverified as written.** The new file and both edited helpers are `_test.go`, and *every* automated budget gate exempts them: `checks/filesize.py` (engineering.yaml `ignore_patterns: [_test.go]`), `TestMaintainability_FileSizeBudget` (explicitly "non-test .go files"), and the complexity gate (`_test.go|test/` in ignore pattern). Even if counted: sibling scale ~150–250 lines; `quota_relay_test.go` 306 → ~316. The claim holds only because nothing measures test files — and repo-wide it's already false (`ed25519_jwt_issuer.go` 539 > 500, reproduced).

**3. No upward-import violations — CONFIRMED, double-safe.** The layer gate skips `_test.go` files entirely (no `layerExemptions` entry needed — and adding one would itself violate the shrink-only rule); independently, test/→auditgovernance/defaultimpl/sso/core is composition(6)→(4/4/5/0), all downward.

**4. Race discipline — AMENDMENT REQUIRED.** Plan §10 steps 3–4 run `-count=1` without `-race`; both `make ci`'s `race` and `make test-e2e` run these exact tests under `-race`, so the plan should too. `-count=10+` is correctly **not** required — AGENTS.md's `-count=10+` is for race *fixes*; these are pure assertion additions in synchronous httptest helpers (no goroutines/`t.Parallel` in `quota_relay_test.go`). `-race -count=1` suffices; billing package verified green under it.

**5. `make ci` — CANNOT pass on the current tree; the plan's step-6 framing is right but must name the reds.** Four pre-existing failures run *inside* `make ci`'s `race` step: FileSizeBudget, DirectorySubdirFanout (root 24 > 21), DirectoryDepth (pipeline's own artifacts), and `TestSdkForm_PARClaimsThreaded` (documented-red interlock, fails in 0.00s). All reproduced. Nested-module rules: unaffected — `cmd/snaplink-billing` is a root-module package (no `go.mod`; also the billing profile's build package), nested modules untouched, root `go.mod`/`go.sum` unchanged. Fan-out ceilings safe: billing at 10 non-test files, sso at 60, delta adds files to neither.

**6. Additional plan drift (new):** step-4's "20 existing green" is stale — the regex matches 22 tests (21 green + 1 red), and as written the command exits non-zero because the red test is inside the regex; pin the file/function name (`billing_form_e2e_test.go` + `TestBillingFormE2E*`) so the step-4 grep agrees with the task naming.

**Bottom line:** the delta design is gate-compliant **after three amendments** — add `-race` to verification steps 3–4, restate "budget ceilings respected" as "test files are exempt from the budget gates; both files stay under 500 even if counted", and make step 6/step-4 explicitly enumerate the four pre-existing reds (the claim "passes make ci" can only mean "introduces no new make ci failures").

Both follow-ups verified: `KeyTenantID = "tenant_id"` is centralized in `shared/core/consts_wire.go:169` (reuse it for the claim — no duplicate const), and `WithMaxTokenBytes` (`options.go:92-103`) is an **inbound-bearer** cap, so a large `roles` claim can't break issuance — but AC-4/AC-5 should still keep the claim set within the enforced RS-side envelope.

**Final verdict: the design is wire-compatible on all four requested dimensions.** The shared-`ed25519Payload`/separate-`ed25519IDPayload` structure makes the tenant_id/roles + ID-token-absent boundary structural (all three signers confirmed through `buildAccessPayload`; ID-token mints source claims from Attributes/§5.5 projection, never the access-token Subject); the introspection projection is fully enumerated with no generic claim iteration, so the no-introspection boundary cannot leak; refresh rotation and token exchange demonstrably preserve AuthTime/ACR/AMR/SID and the `act` chain with mint-time TenantID re-stamping; and `iss` is consistent across token/discovery/RFC 9207 whenever `issuer` is non-sentinel — with the allowlist making the Host fallback structurally unreachable in non-empty mode. Four small pins for the design's final text: (1) update the stale `oauth_token_source.go:43-46` comment in the same change, (2) reuse `core.KeyTenantID`, (3) keep `deriveMeshIdentity` on its fresh fail-open lookup rather than the mint-time claim, (4) document the ServingRegion-vs-tenant_id introspection asymmetry explicitly so a future maintainer doesn't "fix" it.

All worktree facts are now verified. Here is the per-item verdict.

## Verification result

**Preliminary finding: no revised design doc exists.** The deliverable `docs/architect-analysis/cmd-sso-operator-controller-b4-3-adminpaths-sweep-design.md` (mtime `2026-08-07 00:20:57`) predates all three review reports (spec_design_trace `00:24:01`, engineering_gate `00:25:56`, test_design `00:28:57`). No copy, delta, or patch newer than those reports exists anywhere (`find -newer` across `docs/architect-analysis`, git status/stash/branches). The run-dir `task-1-design.md` is only the pre-review delivery summary (different md5, `00:21:08`). The document under review is the *same unrevised version* the reviewers read. Consequently every finding is unresolved — by absence, not by explicit accept/reject.

| # | Finding | Status | Evidence |
|---|---|---|---|
| F1 | Coordinated-rename "only tripwire" overclaim | **Unresolved** | Doc §1:41 "as the only tripwire" and §5 row 1 "the only tripwire that sees it" unchanged. Worktree fact stands: `TestConfigAuditAPI_SnapshotsWiredServesRedactedRunningAppliedDiff` carries committed literals at HEAD:75 (`/api/v1/admin/config/running`) and HEAD:110,132 (`/api/v1/admin/config/cluster-diff`; worktree 144/179, same function); the worktree diff adds zero literal lines (verified `git diff HEAD` — all added lines are const-derived). The drift class is visible to `go test ./...` today with no anchor |
| F2 | Row 11 maintenance-gate justification false; 11 rows, not 12 | **Unresolved** | Row 11 still reads "New test function exceeding 50 lines \| maintenance gate \| Prevented by the two-helper split"; §3.4 repeats "well under the 50-line budget". No gate measures test functions: `maintainability_budget_test.go:71` and `maintainability_complexity_test.go:142` skip `_test.go`; `engineering.yaml` ignore_patterns include `_test.go` and `cmd/`. §5 table has 11 rows |
| F3 | CRD-parity "precedent" does not exist | **Unresolved** | §3.4 still claims "the same precedent as the sibling CRD-parity test's `../crd-ssoconfigdrift.yaml`". Repo-wide grep: zero `.go` files reference `crd-ssoconfigdrift` (yaml tracked, test-less) — planned sibling design only |
| F4/E12 | 14 `TestReconcile_*` package-wide, not 10 | **Unresolved** | E12 row still says "10 package-wide". Measured: 8 in `ssoconfigdrift_controller_test.go` + 6 in `ssoconfigdrift_truthiness_test.go` = **14** function definitions |
| F5 | `assertDocPath` lookup keys/order unspecified; assertion 3 R2-redundant | **Unresolved** | §3.4 lists presence keyed by operator const (assertion 1) then doc-vs-root-consts (assertion 3) with no per-assertion lookup key; §7 case 3's mechanics ("documented key vs `PathAPIPrefix+const` mismatch") is not literal — under the stated order the presence lookup keyed by the renamed operator const misses first. Assertion 3 as specified reduces to R2; neither an independent composition-keyed lookup nor a redundancy statement appears |
| F6 | Operator-side method flip unpinned | **Unresolved** | §5 method-flip row covers only the doc side; nothing pins `http.go`'s GET/POST usage; no residual-gap declaration anywhere in the doc |
| F7 | Row 7 parenthetical conflation | **Unresolved** | Row 7 still reads "(root consts renamed, mount changed, gate behavior changed)" — mechanical const rename + remount passes R3 (const-derived paths); only "mount changed without the consts" fails. Not disentangled |
| F8 | "Root gates do not descend" overclaim | **Unresolved** | §4 blanket claim unchanged. `skipDirs` is name-based (`skipDirs[d.Name()]`, `maintainability_budget_test.go:43–56`) and lists only gen/.claude/web/node_modules/dist/bin/kms/redis/postgres/saml/ldap/extauthz/kerberos/radius/kafka etc. — `cmd` is **not** skipped; the walk descends into `cmd/sso-operator` non-test files today |
| Makefile:263 quote | Missing `go build ./... &&` segment | **Unresolved** | §8 line 2 and §7 preamble still quote `cd cmd/sso-operator && go test -race -count=1 ./...` as the "ci-modules gate, Makefile:263". Actual line 263: `cd cmd/sso-operator && $(GO) build ./... && $(GO) test -race -count=1 ./...`. §1 E9 quotes it correctly — internal inconsistency remains |
| Root-gate caveat | Pre-existing failures uncaveated | **Unresolved** | Closing paragraph still says "Pre-existing conditions: none found". Re-ran `go test -run 'TestMaintainability_|TestArchitecture_' .` today: **FAIL** on all three — `TestMaintainability_FileSizeBudget` (`infrastructure/defaultimpl/ed25519_jwt_issuer.go`, 539 lines), `TestArchitecture_DirectoryDepth` (147 dirs at depth 7–8 under `docs/architect-analysis/auto/runs`), `TestArchitecture_DirectorySubdirFanout` (docs 18, runs 95 > 16; root 24 > frozen 21). §6 step 4 "full gates must pass" and §8 "root gates unaffected" need the caveat |

**No silent drops:** every finding is absent from the doc, but the doc predates the reviews — there is nothing to drop. **No accepts, no rejects:** zero findings carry any accept/reject disposition because no revision was produced. The reviewer-verified core (E15 path fix, tidy mechanics, R2/R3 pins, 8/8 acceptance mapping) remains intact and is not in dispute — the deliverable needs a genuine revision pass implementing the ten dispositions above (reword F1, restate F2/F7/F8, correct E12, delete the CRD-precedent cite, specify `assertDocPath` key order or declare assertion 3's R2 redundancy, declare the F6 residual gap, quote Makefile:263 byte-exact, add the pre-existing-failures caveat).

VERDICT: REJECT — no revised design doc exists; the deliverable is unchanged since 00:20:57, predating all three adversarial reports (00:24–00:28), so all 10 findings (F1–F8, Makefile:263 quote, root-gate caveat) are resolved by neither acceptance nor explicit rejection and remain open with worktree-verified evidence that each is real.

All load-bearing claims independently verified against the tree. Summary of the check:

**Adversarial review stage:** produced no findings — `artifacts/adversarial_review-9c87f3a7/meta/` is empty and the session exited 1. There are therefore no blocking review findings outstanding to resolve or reject; the gate reduces to assessing the design on its own merits.

**Independent verification of the design's central claims (all confirmed by direct inspection):**
- `config/config_load.go` `ServerOptions()` emits `sso.WithIssuer(c.Server.Issuer)` first; `build_app_core.go:154` appends `cfg.ServerOptions()` inside `wireSigningIssuer`; `build_app.go:257` feeds `b.opts` to `sso.NewServer`. The direction's core claim ("never passes WithIssuer") is false — the wiring exists.
- JWT `iss` pinned from the same value: `build_signing_issuers.go:43` `WithEd25519Issuer(srv.Issuer)`; `main_wiring.go:203` `tracing.WithServiceName(cfg.Server.Issuer)` — matching the `issuer_test.go:15` comment exactly (accurate, not drift).
- Discovery override (`applyMFAIssuerSigning` called unconditionally at `server_discovery_config.go:112`, override at :264-265), `resolveIssuer` (server_discovery.go:251-257), `authzErrorBody` RFC 9207 `iss`, form_post hidden `iss` input (form_post.go:38), `finishLoginCodeFlow` `{code, iss}`, and `handleLogin` no-store stamping all present as claimed.
- Coverage claims hold: `config/security_test.go:72-124` (three composition tests), `rootcov_accessors_test.go:246-248` (WithIssuer wins over request base URL), `TestConfigRejectsSDKSentinel` + `config_load.go` sentinel rejection; zero `authorization_code` tests in cmd/sso-server; discovery tests assert presence only; `interfaces/sso` at exactly 60 non-test files; all fixture helpers (`buildApp`, `quietLogger`, `shutdownApp`, `writeBcryptHashFile`, `fetchDoc`, `fetchDiscovery`) and config fields (`ClientConfig`, `PasswordConfig.Users`, `PasswordUserConfig`, `TokenStrategy: "jwt"`, `SkipConsent`, `MethodPassword`) exist, with the minimal-config `buildApp` pattern proven by `cache_ttl_test.go`.
- The design's one evidence correction is itself correct: `git log -S` shows `eb7b6212` (2026-05-12) introduced `sso.WithIssuer(c.Server.Issuer)` in config/ (parent has 0 hits), while `305dc6c0` (2026-06-19) is the verbatim-move refactor — the spec's attribution was wrong, and the design §0 documents this with evidence.

The tests-only scope (one file, ≤500 lines, no production/config/docs changes, no `interfaces/sso` touch) is the correct and minimal response to a direction whose production change is a no-op, and the acceptance mapping is concrete and testable.

VERDICT: PASS - The adversarial review produced no findings, and every load-bearing claim of the tests-only design verifies against the tree (wiring exists end-to-end, JWT/discovery/error-body issuers all derive from `cfg.Server.Issuer`), with the sole evidence error (commit attribution) explicitly corrected and evidenced in design §0.

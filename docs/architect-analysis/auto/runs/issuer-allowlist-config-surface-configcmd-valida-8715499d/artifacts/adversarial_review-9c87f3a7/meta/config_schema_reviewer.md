Audit complete. Report: `docs/architect-analysis/auto/runs/issuer-allowlist-config-surface-configcmd-valida-8715499d/artifacts/config-schema-compat-audit/task-3-audit.md`

## Verdicts

**1. validate-schema loosening scoping — CONFIRMED, mechanism-guaranteed.** The schema is reflection-generated from `config.Config` struct tags on every call (`config/schema/generate.go`), and every struct node carries `additionalProperties: false` (verified in the emitted schema). A key stops being "unknown" only by becoming a struct field — so adding `issuer_allowlist`/`require_configured` to `ServerConfig` changes exactly one node, by exactly two properties. No blanket-loosening path exists. Ran the gates live: `validate-schema` on all three manifest configs exits 1 with exactly `server.issuer_allowlist: unknown field`; the load path exits 0 with the two documented warn-only notices. **One unstated side effect (design's own FM-7):** `RequireConfigured bool` without `,omitempty` will join the `server` node's `Required` list per the `isRequiredCandidate` heuristic (verified: `issuer` etc. are there today) — validator-unenforced, but use `yaml:"require_configured,omitempty"` (precedent: `http2,omitempty`).

**2. Recognized-but-inert conversion — intent confirmed, factual framing corrected.** The keys exist in all 5 claimed files, but `git log --all -S "issuer_allowlist"` returns **zero commits** — they are uncommitted working-tree WIP (HEAD's kustomize base has plain `issuer: sso-server`), and `bin/k8s-rendered/*` is untracked + gitignored. The B4-1 proposal does intend allowlist + `require_configured` as the fleet-safe rollout ("避免一次性破坏多主机"), so the conversion direction is right; "existing fleet manifests" should read "campaign's own pending manifest edits". Also: `config-validate-all` covers only 7 files (none of these manifests, warn-only path) — "CI unaffected" holds; and `bin/k8s-rendered/prod/all.yaml`'s 3 current violations are k8s object keys (`apiVersion`/`kind`/`metadata`), not the allowlist.

**3. configcmd zero-production-change + budgets — CONFIRMED.** `runValidate`→`config.Load`, `runSchema`→`config.Schema()`, `runValidateSchema`→`Schema()+Validate` are all reflection/tag-driven; usage text lists no keys; no golden tests pin `--print`/schema output. `config/` = 26 non-test files, exactly the frozen `directory_fanout_test.go:53` ceiling. `config_load.go` = 498/500 today; the sentinel block (lines 176–185, verified) moves to `validateIssuerPolicy()` in `config_server.go` (195→~235), landing config_load.go at ~489–491 — but the relocation is load-bearing, and the `applyDefaults` "net 0 lines" claim only holds as a condition on the existing `if`.

**4. Docs rows vs schema/OpenAPI — CONFIRMED.** Rows don't exist yet (R6 is a plan); they land in the `## OIDC` table beside `server.issuer` (line 48) and must state array-of-string / boolean types matching the reflection schema. `docs/openapi.yaml` has no config-key or CLI section at all (29 `issuer` hits are all OIDC wire claims), so there is no OpenAPI contract to conflict with — config is governed by `docs/config-reference.md` alone, and the new keys are load-time gates that don't touch the wire `iss` contract.

**Carry-over risks:** FM-7 `Required` heuristic (above), sentinel must stay outside the flag gate (FM-1), the manifest edits share a worktree with unrelated uncommitted changes (compose scope_registry, k8s-distributed consent), and `--print` JSON will gain two fields (byte-compat claim holds for runtime/exit codes, not `--print` bytes).

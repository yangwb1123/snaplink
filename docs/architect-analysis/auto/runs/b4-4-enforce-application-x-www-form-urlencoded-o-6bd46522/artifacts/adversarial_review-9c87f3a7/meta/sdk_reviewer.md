Deep-verify complete. Deliverable: `docs/architect-analysis/auto/runs/b4-4-enforce-application-x-www-form-urlencoded-o-6bd46522/artifacts/deep_verify-7a1f02c1/meta/task-1-sdk-step7-deep-verify.md`. Summary of what I confirmed against HEAD (`18c94576`):

**1. The 7 methods + `_request` — confirmed exact.** `_request` at `docs/sdks/python/client.py:1437-1463` hardcodes JSON (`:1453-1454`); the 7 methods at `:2216/:2236/:2244/:2260/:2280/:2284/:2288` all route through it. `post_revoke_all` sends no body and `HandleRevokeAll` never binds — correctly unaffected. CIBA (`postBackchannel`) is the 8th server op but is absent from `sdk-surface.json` — no SDK method exists for it.

**2. PARRequest nested fields — confirmed, genuinely dependent on finding 1a.** `setFormField` (`oauthwire/bind.go:111-134`) has no `json.RawMessage` case; `[]byte` is silently skipped, and `ValidateAuthorizationDetails` treats empty as "no RAR" (`rar.go:154-156`) — a silent downgrade, worse than an error. Once 1a lands (RawMessage-from-form-string, fail closed), the SDK sends `authorization_details=<urlencode(json.dumps(list))>` / `claims=<urlencode(json.dumps(dict))>` — openapi already documents exactly this for `authorization_details` (`openapi.yaml:15042-15046`); the `claims` property lacks the same note (parity nit for step 6).

**3. MFA flat fields — confirmed end-to-end.** Server has no `Map` case in `setFormField` → `params` silently dropped; `collectMFAParams` (`server_mfa.go:440-453`) folds only flat `code`/`assertion`; both empty → `400 mfa_invalid`. **Two additional silent-drop hazards no review named**: `trust_device` and `approve` are Python bools → `urlencode` emits `True`, but the server bool case matches only `"true"`/`"1"` → silently `false` (an approval silently becomes a denial on `/device/verify`). The SDK helper must lowercase bools and map `params.code/assertion` into the flat fields.

**4. SDK test suite — does not exist.** Zero files import/exercise `client.py` anywhere (`test/`, `examples/`, `ai-dev/`, `checks/`, Makefile pytest targets). "SDK test updates asserting the new 400s" is net-new infrastructure; the design's acceptance row is "make ci + manual". The `SSOError(400, invalid_request)` path itself needs no transport change.

**5. sdk-surface check — not a gate.** It validates only registry ↔ openapi operationIds ↔ capabilities ↔ file existence, never SDK contents or encodings. **Regeneration hazard**: `pyClientHeader` in `cmd/gensdk/gen_py.go` *is* the JSON-only `_request` as a string constant, and the README mandates regeneration after openapi changes — the documented workflow silently reverts the step-7 hand fix. Step 7 must also patch `cmd/gensdk` or amend the README contract.

**6. Missed consumer**: `ops/deploy/openresty/fullstack/static/docs/sdks/client.py` — a second, stale committed copy with the same 7 JSON-encoding methods, publicly served by the fullstack OpenResty deployment (Dockerfile copies `static/docs/` → `/srv/docs/`). The design names only `docs/sdks/python/client.py`; needs an explicit scope decision.

**Verdict**: mechanism confirmed (items 1–3), but "the SDK's own test suite and sdk-surface check gate the change" is not confirmed — nothing automated gates the SDK side — and step 7 needs the gensdk/README amendment and the bool-lowercasing rule to be correct as written.

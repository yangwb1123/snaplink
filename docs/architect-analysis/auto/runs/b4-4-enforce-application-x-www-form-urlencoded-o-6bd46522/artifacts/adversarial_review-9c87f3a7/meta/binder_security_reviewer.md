Review complete. Deliverable written to `docs/architect-analysis/auto/runs/b4-4-enforce-application-x-www-form-urlencoded-o-6bd46522/artifacts/adversarial_review-9c87f3a7/meta/task-1-rawmessage-oracle-review.md` (242 lines, all citations re-verified at HEAD `18c94576`).

## Verdict: PASS with amendments (R1–R7)

**Semantics pinned by the requirement.** The task's "invalid-JSON form values must collapse into the same byte-identical 400 invalid_request envelope as all other bind errors" resolves the earlier store-verbatim ruling into a hybrid: `json.Valid` at bind on non-empty RawMessage values (fail closed), verbatim storage after (CloneRawJSON), empty→absent (mirroring `rawQueryJSON` at `server_login_resolve.go:44-49`), repeated key→first-wins (never `formStringSlice`), shape/allowlist stays at the RFC 9396 gate (`invalid_authorization_details`).

**Four-axis verification — all confirmed at HEAD:**
- **Envelope identity**: `/par` is the only affected endpoint (only seam struct with RawMessage fields: `parRequestForm` :100/:105, bound only at `handle_par.go:65-66`). Every bind error hits the same `core.ErrorBody(ErrInvalidRequest)` branch (:66-69) → `{"error":"invalid_request"}`, deterministic, no description, no trace. Per-endpoint identity holds by construction; cross-endpoint identity correctly not required.
- **Fail closed**: `json.Valid` is a pure scanner, never panics; plain static error → existing branch. In-tree precedent is the shipped `txnParseRequestContext` (`txntoken/grant.go:127-141`): "malformed value is a malformed REQUEST → invalid_request".
- **No side channel**: CT gate stays before `ParseForm` (counting-reader pins zero reads on rejection); `json.Valid` cost is client-bounded post-body-read, same class as `ParseForm`; the one new same-status pair is client-distinguishable only. The existing pin `TestPAR_RejectsMalformedAuthorizationDetailsAtPushTime` (bare object = valid JSON) survives unchanged.
- **No audit/metrics**: `PARDeps` has no recorder; the new error flows through the untouched bind branch — zero observability delta by construction.

**Key conflicts flagged for the design**: the prior "no-parse-at-bind" ruling is superseded for syntax-invalid values (its `TestFormBindRawMessageNoContentParse` inverts; the gate-after-auth 401 pin re-scopes to shape-invalid payloads; transport-parity scopes to well-formed JSON). One side effect documented: the case lands in shared `setFormField`, so the legacy binder's form path also stops silently dropping RawMessage (only other consumer: `interfaces/admin/governance.go:35`).

**Deliverable includes the drop-in replacement rows** for the design's failure-mode table (§5: 8 rows — invalid-JSON bind class, shape-class unchanged, claims asymmetry narrowed, empty/duplicate keys, pre-auth ordering, DPoP interplay, legacy-binder side effect) and acceptance mapping (§6: 8 rows with exact test files), plus the test-plan gap list: openapi `:15042-15050` "Any failure collapses to invalid_authorization_details" text must split syntax-vs-shape (R6), garbage-CT + invalid-JSON fuzz seeds (R5), named counting-reader tests (R2), `/par` byte-identity + `%zz` collapse-lock rows (R4).

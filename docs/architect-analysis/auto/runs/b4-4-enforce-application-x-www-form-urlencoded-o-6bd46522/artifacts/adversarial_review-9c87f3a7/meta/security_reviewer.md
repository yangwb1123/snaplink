Review complete. Deliverable written to `docs/architect-analysis/auto/runs/b4-4-enforce-application-x-www-form-urlencoded-o-6bd46522/artifacts/adversarial_review-9c87f3a7/meta/task-1-oracle-safety-review.md` (261 lines, all citations re-verified at HEAD `18c94576`).

## Verdict: no blocking findings, one material gap

**Q1 — Byte-identical 400 invalid_request: verified.** All 10 bind-error branches and every other `invalid_request` cause on the same endpoint share the exact same envelope call (`errorBody`/`core.ErrorBody`/`authzErrorBody`), bind errors are never serialized (no handler emits the error value), and the malformed-percent-encoding ParseForm failure already collapses into the same class. MFA stays cause-independent (localization keyed on the `mfa_invalid` code). Caveat: sso endpoints use the trace-enriched envelope, protocols endpoints plain `core.ErrorBody` — byte-identity must be asserted per endpoint, not cross-endpoint.

**Q2 — Reject-before-body-read: no timing/error-shape side channel.** Header-only decision; the only same-status pair it creates (CT rejected vs. malformed form, both 400) is distinguishable only by the client's own bytes. Two nuances pinned down: the "body never read" property is binder-level (opt-in `logBodies` debug middleware reads bodies for all requests — tests must use a direct counting-reader unit test), and HTTP/1.1 keep-alive drains unread bodies ≤256KB (standard Go, no action).

**Q3 — no-store + setBearerChallenge: 9 of 10 verified, one material gap (F-A).** `middleware.TokenNoStoreHeaders` sets both headers (`no_store.go:22-26`), called before bind on 9 endpoints — but **`handleDeviceVerify` (`server_device.go:223`) never calls it**. The design's ledger cites only 9 line refs for its "all 10" claim (`server_device.go:42` is device *code*); the design's own no-store acceptance row would fail on device verify. **Required one-line fix (H8)**: add `tokenNoStoreHeaders(ctx)` at the top of `handleDeviceVerify` inside the design's step-1 seam edit (also repairs the pre-existing missing headers on that endpoint's 401/400 paths). setBearerChallenge is preserved by construction: no seam endpoint challenges on 400 paths, and the device-verify bearer gate runs before bind.

**Q4 — Constant-time + DPoP/mTLS/PKCE: untouched.** All cited sites confirmed at HEAD (`client_secret.go:19-31`, `code_store.go:167,207`, `mesh_authz.go:443`, `server_native_sso.go:102`, `dpop.go:130,243`); all are downstream of or off the bind path. DPoP capture (`server_token.go:215-266`), mTLS extraction (`:258-260`), and PKCE (`token_request.go:22` → `token_authcode.go:186-190`) are untouched by a byte-identical form path.

**Hardening amendments H1–H7** (test-level, folded into the design): shared-`requestContentType` parity matrix (the §1.1 snippet duplicates normalization inline — drift risk), CT-check-before-ParseForm ordering proof, per-endpoint byte-identity assertions, no-observability-divergence constraint (no new audit/metrics on rejection), malformed-form collapse lock (`%ZZ` ≡ JSON rejection), garbage-CT fuzz seeds, and DPoP-interplay documentation (JSON+DPoP now gets `invalid_request` without a nonce; proofs are CT-independent and reusable on the form retry).

Pre-existing items reported separately: the old-contract fuzz seeds and `TestFormEncoded_JSONStillWorks` (both already sequenced in the design), plus a pre-existing out-of-scope `error_description` leak at `handle_par.go:223` (`invalid_authorization_details`).

All blocking findings independently verified against the merged code at HEAD (`7884230e`):

**Security reviewer** — (3) hard gate resolved: step-2 note explicitly scopes out the internal-mint check ("This example covers only request-borne scopes; the internal-mint registry check is intentionally not shown — a handler that composes scopes beyond the request MUST run `scoperegistry.RejectUnregistered` on the effective set before issuance, exactly as `token_client_credentials.go` does (the dispatch seam cannot see them)"). (2A) resolved: "same guard discipline as the AMR claim" with non-empty-only semantics, consistent with the `issue_payload.go:83` source comment ("same guard+copy discipline as AMR — the claim is emitted only when non-empty"). (2B) resolved: step-3 distinguishes the fail-open `accessors_handlers.go` conditional-access call site from the roster-based `subjectRoles` mint path, with the `role.Code` projection note.

**Generator-contract reviewer** — F1 resolved: zero `file.go:N` line-numbered citations in the template body (grep-verified, including de-referenced `token_client_credentials.go`). F2 resolved: zero backticks between the raw-string delimiters at `templates_handler.go:150/:278`. F3 resolved: "The 500 on accessor failure is deliberate: minting without roles would silently under-claim the token, so unlike the fail-open advisory path this example fails closed."

**Test-plan reviewer** — `h.Roles(` ordering marker at `scaffold_contract_test.go:221` (struct TODO and `permissions.Provider.Roles` citation can no longer satisfy it); `Roles:\s+roles` presence check via `rolesClaimRE` at :194/:241. `assertKindInvariants` dispatcher replaces the handler-kind guard; `TestGeneratedScaffoldsCompile` runs it at `scaffold_build_test.go:131` before the hermetic build+vet loop.

**Sequencing & red-first** — commit A `3f0dd692` carries the 290-line `scaffold_contract_test.go`; commit B `7884230e` touches exactly the 3 grant files (no `cmd.go`/`verify.go`); red premise independently confirmed: the pre-B template held only the banned `}, scopes)` tail (:208) and TenantID pin (:207) — zero absent markers — so the helper fires named failures red-first, green after.

**Gates (fresh)** — `go build ./... && go vet ./...` PASS; `go test ./cmd/sso-ctl/generate/ -count=1` PASS (all 4 kinds incl. grant + build/vet loop, `TestRunExitCodes` 2/1/0, `TestVerifyGeneratedBuildVet`). The `TestMaintainability_/TestArchitecture_` failures are pre-existing (`ed25519_jwt_issuer.go` 539 lines not touched by A/B; root fan-out from untracked campaign docs), matching the gate_verifier's stash-proven baseline. Only cosmetic nit: the auditor's "verbatim guard+copy" description vs. the file's "same guard discipline" — satisfies 2A's actual requirement, non-blocking.

VERDICT: PASS - every blocking finding (security internal-mint scoping, AMR wording, mint-path distinction, de-referenced citations, backtick hazard, fail-open divergence note, `h.Roles(`/`Roles:\s+roles` tightening, A-then-B sequencing) is resolved in the landed code with byte-level and empirical gate evidence; remaining gate failures are proven pre-existing and untouched.

Verification complete. Report written to `docs/architect-analysis/auto/runs/add-boundary-anchored-segment-verification-ancho-d746c8c5/artifacts/deep_verify-4b91c2e8/meta/task-1-tamper-core-deep-verify.md`. No repo code touched; scratch harness lived in `/tmp`. Summary of the four checks, all exercised against the **real** chainer (`audit.Recorder` + `WithHashChain`) and the **built binary**:

**(a) Order normalization — sound; dual-order claim needs one precision fix**
- Every crafted input fails closed at kernel + binary level: front-truncation (`chain break at index 0`), interior tamper (`hash mismatch`), non-contiguous, forged-genesis on newest-first (`hash mismatch at index 0`), wrong anchor, and the both-ends-anchor ambiguity craft (fails both orientations; passing would require a preimage of the anchor). A wrongly-reversed genuine chain cannot verify — continuity binds order cryptographically. The `--limit` truncation path can't be masked (the `truncated` flag is computed before any reversal; design maps it to exit 1).
- **Finding (non-blocking):** as literally specified (retry with the *same* anchor), dual-order accepts exactly the set the normalization accepts — it does not expand acceptance. The masking is real only when the retry *degrades* the check, which I demonstrated: genesis-fallback accepts a genesis chain under any non-empty anchor (vacuous boundary assertion → false exit-0 "anchored" claim), and integrity-fallback accepts front-truncated and non-contiguous input. Decisions 5/7 reject both, so the design's behavior is fail-closed; recommend sharpening decision 5's rationale wording at implement time.
- Inherent scope (not a gap, documented in chainer.go): full suffix recompute and file-level tail truncation are undetectable by a *start* anchor — `--checkpoint` is the complementary head anchor.

**(b) Exit/output contracts — all pinned, robust to malformed bundles**
- Empirically: unanchored mid-chain segment → exit 1 `chain break at index 0 ... expected ""`; anchor mismatch → exit 1 with `expected "…"` (token only reachable if the anchor really seeds the walk); `--limit` truncation today → **exit 0 with the false tip** (defect reproduced: `chain verified: 3 event(s), head=<prefix head>`), which the design's exit-1 `prefix verified ... not the full chain` fixes; empty+anchor → exit 1 (design F6); misuse → exit 2.
- Malformed bundles (`{}`, `{"events":null}`, garbage, numeric prev_hash, `[null]`, missing file) all resolve to unambiguous exit 1; `null`/`[]` take the documented legacy empty path; exit 2 unreachable from bundle content. One impl note: `--anchor-hash ""` vs absent needs `flag.Visit` for AC-8.

**(c) No `VerifyEventIntegrity` fallback — confirmed, and required**
- The CLI calls only `VerifyChain`/`VerifyChainAgainstCheckpoint`. Kernel demo proves the fallback would be unsound: non-contiguous and front-truncated input both **pass** `VerifyEventIntegrity` while failing `VerifyChainSegment`. The one legitimate use stays scoped to auditexport's `!Contiguous` bundles (auditexport.go:164-166).

**(d) Legacy byte-identical + AGENTS.md invariants — confirmed**
- Untruncated legacy outputs pinned byte-identical (`chain verified: %d event(s), head=…` exit 0, `no events to verify` exit 0, `chain BROKEN:` exit 1); the only unanchored change is the direction-mandated truncated row; `--checkpoint` paths untouched (tests pass). No store lookups (package imports only stdlib + `platform/audit`), no new endpoints, no new `Err*`/OpenAPI/config surfaces; fail-closed exit mapping verified, including the CLI-level empty+anchor exit 1 that overrides the kernel's trivial empty-pass.

Gates: `go build ./...` and `go vet ./...` clean; `go test -race` on `auditverify`, `platform/audit`, `auditexport` pass; the maintainability/architecture run fails only on **pre-existing** fan-out drift from untracked parallel-campaign dirs (root 24>21, `runs` 84>16, `docs` 18>16) — unrelated to this run.

All reviewer claims are now independently confirmed against the tree. Here is the final status of every actionable finding versus the design at `docs/architect-analysis/auto/runs/cmd-sso-ctl-auditverify-discovery-truthiness-and-endpoint-hardening-sweep-e0e1fbf0/artifacts/design-e1bc63fa/task-1-design.md` (the only design artifact; no amended revision exists — dir mtime 09:22, no newer files).

## Finding-by-finding status

| Reviewer finding | Required | Verified status in design |
|---|---|---|
| **oauthwire `bind_test.go` + AC-6 matrix** (gate P3; acceptance item 1 — "the single clearest omission") | Lock the `normalizeContentType` extraction (case, charset, comma-list, quoted-CT) | **UNRESOLVED.** `grep -c bind_test` = 0; `protocols/oauth/oauthwire/` has no `bind_test.go` (11 files, only `auth_code_handler_test.go`, `client_creds_test.go`, `fuzz_test.go`, `refresh_token_test.go`, `token_exchange_helpers_test.go`); §7 step 5 lists only the bind.go edit |
| **Config-layer + build-level wiring acceptance** (acceptance item 3; e2e P3 — "the only genuine coverage gap found") | `config/security_test.go` parse/default-false row + `TestBuildApp_OAuth21StrictModeFlipsDiscovery`-style pin (precedent verified at `cmd/sso-server/oauth_stores_test.go:162`) | **UNRESOLVED.** `grep -c security_test` = 0; `config/security_test.go:37` `TestSecurityConfig_Parses` exists but is never referenced; §6 has no config row; §7 step 8 wires production only, no test |
| **governance.go:235-236 stale-comment fix** (wire reviewer "one actionable finding"; acceptance item 4) | REQ-2 removes `missing_token` from middleware.go → comment "EXISTING admin_auth_not_configured / missing_token / forbidden / rate_limit_exceeded literals in middleware.go" (verified at governance.go:233-240) goes stale in the same change | **UNRESOLVED.** `grep -c governance` = 0; §2.3 docs sweep lists only error-codes.md:957 and openapi.yaml:13056-13057 |
| **§8 baseline capture, identical-failure-set bar** (e2e P4) | Name the measured baseline (539-line `ed25519_jwt_issuer.go`; root 24>21; docs 18>16; auto/runs 284>16 — all re-verified: 539, 34/35 dirs, 18/19, 284/285) and require identical failure set | **UNRESOLVED.** `grep -c baseline` = 0; §8 still defers to the stale strict-mode verification §5 (2-item summary), no verbatim-capture instruction |
| **Permissive-contrast leg: explicit per-leg pin** (gate P4; acceptance item 2) | `permCode == 400` **and** `permBody ==` literal 28-byte golden, vacuous-guard comment | **PARTIAL.** §6 has cross-response equality ("byte-identical to the strict 400") and §4 has the vacuous rationale, but no explicit per-leg status+golden assertion or test-code guard comment |
| **options.go ≤9-line constraint named** (gate P2) | Name the hard ceiling + 60/60 no-fallback risk | **Implicit only** (sketch is 8 lines → 498 ≤ 499, so it conforms); not named — low risk |
| **REQ-3(d) exit-2 re-scope** (cli reviewer FLAG) | Re-scope to "unasserted keep-green" or convert to `checkMisuse` | **RESOLVED** — design D5/§6(d) explicitly says "kept green, not in-process asserted" (option a) |
| Security engineer's 2 observations in failure-mode table | Record RFC 6750 §3.1 deviation + admin-401 no-store | **Not recorded** (non-blocking recommendation; §4 covers the convergence rationale only) |

## Conclusion

Four of the reviewers' "needed before handoff" additions (acceptance reviewer items 1, 3, 4 + e2e P3, P4) are **completely absent** from the design — zero mentions, not even an explicit rejection with evidence — and the permissive-contrast leg is only partially strengthened. The design has not been amended after review. The verified facts underneath (oauthwire file list, governance.go comment, config/security_test.go:37, oauth_stores_test.go:162 precedent, 539/34/18/284 baseline counts) all match the reviewers' claims. Only REQ-3(d) was genuinely resolved via re-scoping.

VERDICT: FAIL - the design does not resolve four blocking review additions (oauthwire bind_test.go AC-6 matrix, config/security_test.go + cmd/sso-server build-level wiring test for strict_credential_content_type, governance.go:235-236 stale-comment fix, §8 verbatim-baseline identical-failure-set bar) nor the permissive-contrast per-leg golden pin, and none are explicitly rejected with evidence.

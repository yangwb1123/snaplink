All cited locations verified. The revision is complete and written to the pipeline output path.

## Summary

**Deliverable**: rewrote `docs/architect-analysis/auto/runs/make-iss-a-configurable-allowlist-and-stop-host--d9077730/artifacts/design-a77de8a6/task-1-design.md` (the 20-line summary is preserved in `DECISIONS.md`), replacing it with the full revised design. Every claim re-verified against the tree at HEAD `e217aa13` before writing.

**The three refuted claims, resolved:**

1. **12 RFC 9207 sites** — Materialized the complete Family-A table (A1–A12): `authzErrorBody` family (`server_discovery.go:265/278/290`), providers/home-realm (`server_oauth.go:468/481`), silent renewal (`handle_silent_renewal.go:369`), consent challenge (`server_logout.go:259`), MFA challenge (`server_mfa.go:139`), direct-mint/code-JSON/JARM (`server_finish_login.go:191/456/461/469`), form_post (`server_discovery.go:343`), JARM error (`origin_validation.go:163/176`), MFA-complete errors, native-SSO (`server_native_sso.go:225`) — all reachable only from G2/G3/G4, zero edits to `server_finish_login.go`. Gate predicate pinned: `denyUnconfiguredIssuer()` = non-empty allowlist ∧ (issuer `""` ∨ SDK sentinel ∨ non-member), plus the 7-row reachable-state matrix and the `require_configured_issuer` cmd-path redundancy stated explicitly.

2. **~29 ungated consumers** — Decision: **choke point + scoped claim** (reviewers' option b): `resolveIssuer` returns `""` under deny, covering B2–B10, C2–C6, D1, D3 in one function; a second choke point (`buildOIDCConfiguration` deny branch) covers C4 federation, which renders issuer outside `resolveIssuer`. The claim is re-scoped ("no Host-derived issuer anywhere; gated 503s, ungated empty values") with the residual config-state fingerprint documented; `setBearerChallenge` is deliberately *not* routed through deny (free function, no Server access, no added guarantee).

3. **Normalization + ops + accessor + sentinels + acceptance table** — Normalization pinned membership-only with the four raw stamping sites named and a 9-row edge-case rule table (IDN, default ports, trailing dot, duplicates, etc.). `ops/deploy/kustomize/base` + rendered dev manifest updated to `http://127.0.0.1:8080` in the same change (dev-only value change, atomic; prod overlays already https). TokenIssuer gap closed via optional `core.TokenIssuerIssuer` + `Issuer()` accessors in a new `defaultimpl/issuer_accessors.go` (avoids the 539-line pre-existing over-budget file). Both sentinels named (`snaplink-sso` = SDK unset → runtime 503s; `sso-server` = cmd non-member → boot panic). A-1..A-16 materialized with verified test homes, plus revision rows A-17..A-22; budgets re-tabulated (sso.go +1 = exactly 500, server_finish_login.go +0, server_token.go +1 of 5 headroom, config_load.go −5 via sentinel-block replacement, interfaces/sso still 60 files).

Also folded in: `ValidateIssuerConfig` wired into `config.validate()` (kills the sibling landing-order preflight blindness), migration revised with issuer-value changes as explicit fleet-atomic steps and "key-removal" defined as rollback-only, and the three AGENTS §5.6 contract-doc updates. Pre-existing gate failures (`ed25519_jwt_issuer.go` 539 lines, pipeline artifact tree) are reported separately, not fixed.

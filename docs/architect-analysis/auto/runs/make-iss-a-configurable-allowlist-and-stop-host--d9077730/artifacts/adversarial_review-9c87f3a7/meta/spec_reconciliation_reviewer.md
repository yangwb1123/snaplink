Reconciliation complete. Deliverable: `docs/architect-analysis/auto/runs/make-iss-a-configurable-allowlist-and-stop-host--d9077730/artifacts/design-a77de8a6/task-1-reconciliation.md` (following the prior run's `task-1-reconciliation.md` naming precedent). No production code touched. Every claim re-verified against HEAD `e217aa13` + the dirty worktree (loader flow, `--validate-only`, reflection schema, all deploy configs, the three docs' current state).

**R1 — Contradiction resolved (option (b) adopted).** The sibling non-goal protects two things that survive presence-guarded wiring: keyless configs load byte-identically (nil allowlist + require off ⇒ no-op, same class as `oauth_21_strict_mode`), and the unconditional strictness gate stays in `runValidate`. `core.ValidateIssuerConfig` runs inside `config.validate()` so boot, `--validate-only`, `make config-validate-all`, and `sso-ctl config validate` all agree by construction — preflight is live from L1, killing the "preflight green, boot panics" class. The unlanded sibling spec gets a text amendment: non-goal becomes "no *unconditional* enforcement inside `config.Load`/`validate()`". Explicitly supersedes the prior run's R4-L2 ruling ("validate() deliberately NOT given entry-validity"), whose sibling-first premise never materialized.

**R2 — Landing order pinned.** L1 server change (with option (b), no deploy-config value changes — all 10 in-tree configs stay green) → L2 fleet-atomic issuer-value remediation for the 3 remaining literal-issuer configs (the only wire-breaking step; pin coordination, `token_ttl` window, boot/log-only canary) → L3 sibling gate (target-state gate). "Key-removal" is renamed: rollback = config-diff revert, gated on the value-change flag; no cleanup step removes enforcement keys from a live deployment.

**R3 — Gate-posture mismatch documented.** Three facts pinned with exact required wording (carried into the config-reference rows, sibling §9, design migration): validate exit 0 with no keys proves nothing about posture (legacy Host-fallback-capable mode stays live); after L3 it is still a file gate, not a deployment attestation; `--validate-only` never constructs the server, so strict-mode panics and G1–G4 503s are never exercised by preflight.

**R4 — AGENTS §5.6 items in scope, acceptance-pinned (A-17..A-19).** `issuer_not_configured` 503 row in `docs/error-codes.md` (new subsection under Authentication); both `server.issuer_allowlist` / `server.require_configured_issuer` rows plus the amended `server.issuer` row in `docs/config-reference.md`; openapi 503 additions at the verified insertion points — `/token` (`:1203`) and `/auth/login` (`:598`) 503 descriptions amended, `/auth/mfa` (after `:698`) and discovery (after its `"304"` at `:3540`) get new 503 blocks.

Consequential pins (R5): http entries legal for any host (resolves the kustomize-base dev-config break *by rule*, aligning with the sibling's R2-1); cmd-path violations become load errors (sentinel precedent), SDK stays panic + 503s; the deny predicate and 12-site enumeration; residual ~29-surface non-goal + TokenIssuer limitation; the 3 allowlist configs added to `make config-validate-all`. Pre-existing gate failures (`ed25519_jwt_issuer.go` 539 lines, pipeline artifact tree, `cmd/sso-ctl` fan-out) reported separately per AGENTS.md.

I have completed an independent verification pass against the tree at HEAD `eb85a1d5` (dirty worktree in-flight). Here is what I found.

## Verified resolutions (blocking findings → evidence in tree)

| Review finding | Resolution | My verification |
|---|---|---|
| Wire §1: preflight blindness until sibling lands | Option (b): `core.ValidateIssuerConfig` presence-guarded inside `config.validate()` | `config.validate()` is the single loader gate (`config/config_load.go:144`); `--validate-only` (`cmd/sso-server/main.go:96-101`) and `configcmd validate` (`cmd/sso-ctl/configcmd/main.go:99` = `config.Load`) both funnel through it. Confirmed |
| Wire §5: issuer-value change is the single wire-breaking step | Design §8.3 + runbook §2.1/§2.4/§6/§7: fleet-atomic, pin graph, `token_ttl` window | Runbook §2.1 inventories the 4 literal configs with verified line refs (`cmd/sso-server/config.yaml:19`, `bin/config.yaml:17`, `ops/deploy/k8s/config.yaml:11`, helm `values.yaml:162`) |
| Wire §5: "key-removal" ambiguity | Rollback-only, gated on value-change flag (design §8.5, runbook §8 R1/R1′/C1) | Confirmed present |
| Wire §6: contract docs absent | Design §9 + reconciliation R4, acceptance-pinned A-22 | Verified absent at HEAD: 0 hits for `issuer_not_configured` in error-codes/openapi; no allowlist row in config-reference (`server.issuer` at `:48`); openapi 503 blocks exist at `:598`/`:1203`, discovery `"304"` at `:3540` — insertion points real |
| Wire §2: `validate-schema` hard-fails deploy configs, absent from CI | R5-6 + runbook §2.2 + A-20 | Confirmed: all 3 allowlist-carrying configs carry `issuer_allowlist` today; `make config-validate-all` list (Makefile:400-410) excludes all three |
| Wire Edge 1/2/3: SDK-path leaks, require redundancy, inert allowlist | Choke point + scoped claim (§5.1-5.3); reachable-state matrix S1-S7; explicit "require is a no-op on cmd path" statement | `resolveIssuer` fallback verified (`server_discovery.go:251-255`); matrix internally consistent |
| Security: 8→12 RFC 9207 sites; predicate unpinned; sentinels conflated; normalization; TokenIssuer accessor gap | §2 A1-A12 table; `denyUnconfiguredIssuer` pinned; both sentinels named (`consts_oauth.go:139` = `snaplink-sso`, `config_load.go:60` = `sso-server`); membership-only normalization with 4 raw stamping sites; optional `TokenIssuerIssuer` + A-18 | Spot-verified emission sites (`server_finish_login.go:191`, `server_native_sso.go:225`, `server_mfa.go:139`, `origin_validation.go:163/176`), G1-G4 placements, `buildBaseMetadata` (`server_discovery_config.go:144`), override (`:264-266`), `spi.go:245` TokenIssuer (Issue/Validate/Revoke only), `serverbuildsign/build_signing_issuers.go` builds all three from `srv.Issuer` |
| Acceptance: mapping absent; fallback "untested" refuted; budgets | A-1..A-22 materialized; `rootcov_flow_test.go:235-236` pin; §10 budget table | Acceptance table present in design §12; fallback pin verified (`interfaces/sso/rootcov_flow_test.go:236-238`); every line count verified exactly (sso.go 499, server_finish_login.go 500, server_token.go 495, config_load.go 498, consts_oauth.go 188, spi.go 488; `interfaces/sso` non-test = 60/60; defaultimpl 26); pre-existing `ed25519_jwt_issuer.go` 539 lines confirmed |
| Migration: runbook missing | Delivered with §1-§8 | Verified on disk with matching line refs |

## Blocking defect: two final artifacts mandate mutually exclusive implementations

The run's own design directory contains a direct contradiction, and neither artifact declares the other superseded on it:

- **`task-1-design.md` (rev 2) + `migration-rollback-runbook.md`**: `IsAbsoluteIssuerURL` = http for **loopback only** (§4.2/§7); kustomize base + `bin/k8s-rendered/dev/all.yaml` **must be edited** to `http://127.0.0.1:8080` in the landing commit (§8.2); runbook §2.3 **explicitly rejects** "relax R5 to permit http for any entry"; require-on + empty allowlist ⇒ panic always (rule 3, S6, A-10).
- **`task-1-reconciliation.md` (same artifacts dir, final stage)**: R5-1/A-4 rule **http is legal for any host** ("the design's R5 loopback-only framing is **dropped**"); "kustomize base config needs **no edit**"; L1 pinned as "**No deploy-config value changes**"; require-on + nil allowlist ⇒ error only if the issuer is not an absolute URL (R1 table — contradicts S6/A-10); `config_load.go` delta `+1 → 499` (design says `−5 → 493`).

I verified the worktree: `ops/deploy/kustomize/base/config.yaml:12` and `bin/k8s-rendered/dev/all.yaml:22` carry `issuer_allowlist: [http://sso-server.snaplink-sso.svc.cluster.local:8080]` (http non-loopback) **today**. Under the design's rule that entry is illegal and the §8.2 edit is mandatory; under the reconciliation's rule it is legal and the edit is forbidden. An implementer following the reconciliation lands without the remediation and the first dev deploy panics at boot (the runbook's own §2.2 RED analysis); the acceptance pins A-20 ("post-update" config) vs reconciliation A-4 ("no edit") are mutually exclusive. The reconciliation's amendment list claims its rule was applied to the design — factually false against the final file — and no artifact records an explicit rejection of R5-1.

## Minor citation drift (non-blocking)

`rootcov_flow_test.go` and `login_early_gate_headers_test.go` live in `interfaces/sso/` (not `test/`); `build_signing_issuers.go` is `cmd/sso-server/serverbuildsign/`; `test/iss_response_test.go:90/122` already pins login-error `iss == request base URL`, so the design's "error-path iss is non-empty-only today" claim (A-14) is stale in one spot. The requirements doc's "(fallback branch untested)" was corrected by the design revision, as claimed.

VERDICT: FAIL - every substantive review finding is resolved with verified evidence except one blocking contradiction: the final design and migration runbook (loopback-only http, mandatory kustomize/base edit, panic-on-empty-require) and the same run's reconciliation (http-any-host, no config edit, lenient require) mandate incompatible implementations of the shared `IsAbsoluteIssuerURL`/`ValidateIssuerConfig` contract and incompatible L1 change scope, with neither ruling explicitly rejected or marked superseded; the record must pick one resolution and pin the other as rejected before implementation is deterministic.

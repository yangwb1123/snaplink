Done. The design artifact at `docs/architect-analysis/auto/runs/b4-2-register-the-scope-matrix-v2-table-as-a-glo-b1a061ad/artifacts/design-a77de8a6/task-1-design.md` no longer contains bare counts — the "8 steps" and "9 failure modes" are now fully specified, and I folded in the review corrections that were blocking implementation.

**What was verified before writing** (HEAD is now `faf05452`; diff against evidence commit `ea351a86` for all touched files is empty, so every prior citation holds): the seam region (`server_token.go:121-125` split → `denyTokenScopeCombo` :128), all 8 minting branches with their effective-scope points (authcode `info.Scopes` :273, refresh `refreshResolveScopes` :359-365, device :199, CIBA :282, exchange direct `boundScopes` :343 + guest :453, cc :34-38, jwt_bearer :88, saml2 :96 via `customGrantHandlers`), fan-out ceilings (`cmd/sso-server` 24, `config` 26, `interfaces/sso` 60, `protocols/oauth` 12), layer classification, and the matrix constants. Two citations were corrected: the bypass constants are at `oauthvalidate/scope.go:117` (not :103-107), and `serveWithClaims` is `cmd/snaplink-billing/auth_test.go:40,83`.

**The document now contains:**

- **§1 wire-compat baseline** — the byte-level invariant: every `/token` response's status/body/headers identical to baseline, with the plain-`core.ErrorBody` (no `trace_id`) shape pinned as the oracle-safety property.
- **§4 the 8-step migration sequence** — (1) split-first budget prep into existing `options_admin.go`; (2) unwired `scoperegistry`+`scopecontract` packages; (3) inert config surface; (4) option + dispatch seam pinned *before* `denyTokenScopeCombo`; (5) per-branch effective-scope checks closing the store-bound gap; (6) `cmd/sso-server` wiring in existing `build_stores.go` (the invalid `build_app_trust.go` split target is dropped — 24-file frozen ceiling); (7) T-8(d) e2e acceptance; (8) contract docs + final gate. Every step has verification gates, rollback criteria, a byte-compat statement, and ends in `make ci`.
- **§5 the 9 failure modes** — FM-1 config-drift divergence (unconfigured replica keeps old behavior), FM-2 mixed enabled/disabled replicas, FM-3 `extra_scopes` blast radius (fail-closed validation, boot failure, registry-only feeding), FM-4 OIDC standard scopes (now a deterministic built-in set `{openid, profile, email, address, phone, offline_access}` + `device_sso`, covering webauthn-minted `offline_access`), FM-5 wildcard semantics (`:*`-only, matching `permissions.Matches`), FM-6 pre-wired authcodes/refresh chains (drain window = remaining refresh TTL), FM-7 build-once race discipline, FM-8 multi-replica snapshot skew, FM-9 body-shape/seam-ordering drift. Each has trigger, observable effect, blast radius, mitigation, and a verification pin.
- **§6 the 6 acceptance rows** mapping T-8(d) to named tests, and **§7 budget discipline** restating the gate-clean placement constraints.

Documentation-only change — no `.go` edits, so the mandatory Go gates don't apply here.

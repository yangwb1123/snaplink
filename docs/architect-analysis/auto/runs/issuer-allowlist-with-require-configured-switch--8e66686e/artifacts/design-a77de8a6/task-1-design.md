Design delivered to `docs/architect-analysis/auto/runs/issuer-allowlist-eliminate-the-host-derived-fall-bbdb82eb/artifacts/design-a77de8a6/task-1-design.md`.

## Verification result: 16 claims confirmed, 1 refuted, 1 omission

**All 8 citations confirmed** with exact line evidence: `resolveIssuer` Host fallback (`server_discovery.go:252-255`), discovery `Issuer: base` + `WithIssuer` override (`server_discovery_config.go:144,260-266`), `BaseURL` trusted-edge gate, non-sentinel default/sentinel rejection/`WithIssuer` wiring (`config_load.go:60,183-184,305-309`), `build_app_core.go:154`, `WithIssuer` single-string (`options.go:349-351`), `oidc_discovery_test.go:80`, SAML `AllowedIssuers` (different concern). All budget numbers (456/483/499/490/500/500/487/488/495/499/498/340), the 60-file ceiling, the 8 RFC 9207 sites, the signer-constructor JWT `iss` path, and the security-test pins checked out exactly.

**Refuted — blocking correction (C1):** the spec's central finding "handleLogin is the sole authorization entry" is **false**. `handleMFAComplete` (`POST /auth/mfa`, `server_mfa.go:181`) reaches `finishLogin` via `resumeLoginAfterMFA` → `finishLoginWithDeviceTrust` without passing `handleLogin` — so in strict-unconfigured state it could still emit a Host-derived `iss` in an authorization response. I traced `wrapAuthorizationResponse`'s four callers to prove the complete entry set is exactly `{handleLogin, handleMFAComplete}`; the design adds a third gate (G3) at `handleMFAComplete` (487 lines, 13 headroom — fits).

**Omission (C2):** `ops/deploy/kustomize/{base,overlays/prod}` and `k8s-distributed/config.yaml` **already carry `server.issuer_allowlist`**, currently warn-and-ignored by `decodeStrictWithFallback` (`config/source.go:264`). The design converts this into a real knob and adds acceptance A15a (switch-off deployments stay byte-identical, warning disappears).

**Design contents:** two additive config keys + two conditional options + `ErrIssuerNotConfigured`; boot-time `validateIssuerConfig` (panics on cmd path, 503s on SDK path); four one-line gates (discovery placed *before* the body-cache lookup, C3; credential-endpoint 503s structurally no-store via header-first ordering, C4); normalization shared with the sibling configcmd gate; oracle-safety and fail-closed/fail-open classification per AGENTS.md §3; 6 failure modes with recovery; 5-step migration; 18 testable acceptance cases (A1-A16 + two new) mapped to concrete test homes; budget table; verification commands.

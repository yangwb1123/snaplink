Review complete and written to `docs/architect-analysis/auto/runs/b4-1-claims-contract-emit-tenant-id-roles-from-c-d6dcb22b/artifacts/adversarial_review-9c87f3a7/meta/protocol_security_reviewer.md`. Summary of what I verified against HEAD (`d0a697bd`):

**1. RFC 9207 invariant — holds today; consolidation is the right fix**
- `resolveIssuer` (server_discovery.go:251) and the discovery override (server_discovery_config.go:264–267, mis-homed inside `applyMFAIssuerSigning`) implement *identical* logic; exactly one `cfg.Issuer =` assignment site exists. Equality holds for every request today, including discovery-cache hits and the federation projection (`BuildOPMetadata` derives from `buildOIDCConfiguration`). The mis-homing is a structural hazard, not a current divergence.
- For "by construction" to be true, the design must pin: `resolveIssuer` **delegates** to `effectiveIssuer(requestBaseURL(...))` (not re-implements), the override block is **deleted** from `applyMFAIssuerSigning`, and a three-way equality test is added (existing anchors `iss_response_test.go`, `oidc_discovery_test.go:80`, trusted-proxy trio don't cover all issuer states × bases).

**2. tenant_id/roles vs RFC 9068/OIDC** — no flat-namespace collisions (verified field-by-field in `ed25519Payload`); both are private claims, ID-token isolated by struct separation, OIDC Core §5.4 required claims untouched. Refresh preserves tenant_id by re-binding `client.TenantID`. **Three required pinnings found:** (a) `tenantHintFromClaims` (admin quota) reads `Extra["tenant_id"]` and would silently ignore the new first-class claim — must migrate; (b) exchange roles must resolve with the **local** (pre-pairwise) subject, else pairwise deployments mint tokens with zero roles; (c) exchange tenant_id is *re-derivation* from the exchanging client, not copy-through — must be stated.

**3. client_credentials no-roles** — verified the only client-as-subject mint; the 7-of-8-interface exclusion is structurally sound (subject `Roles` must also stay nil, not `[]string{}`, for byte-omission).

**4. Allowlist 503 oracle safety** — the mechanism (single raw-string map lookup, check-first in the handler, boot-only URL validation) is oracle-safe by construction; the summary pins none of the response shape. Required: no-store+Pragma headers, no ETag/If-None-Match, plain `errorBody` (no `iss` echo, no bearer challenge), explicit error-code choice (the "no error-codes changes" constraint conflicts with AGENTS.md), and **surface scope** — the opt-in federation entity-config endpoint serves a *signed* statement with request-base-derived endpoints (`domains/federation/handler.go:80–120`) and RFC 9728 RS metadata (`server_resource.go:170–195`) bypass the gate; either gate the shared projection or enumerate exclusions.

**VERDICT: FAIL** — all four invariants verify against HEAD and the design's citations are accurate, but the artifact is only the 17-line summary and seven security-relevant details are absent from it (listed as findings 1–7). These must be pinned before implementation.

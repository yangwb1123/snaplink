Verification complete. I traced every surface the change touches against the actual contract docs, the docscheck drift gates, and the design text. Verdict first: **the design's "no documentation updates" is correct for the four `docs/` contract files the task names — but only conditionally — and it misses the repo's actual authoritative "sso-ctl usage docs" (the package doc comments), where three concrete updates are required.** The design says "usage banners" but never mentions the package docs.

## A. Contract documents the implementation MUST update (sso-ctl usage docs)

There is no `docs/sso-ctl.md`; the user-facing CLI usage contract lives in the in-binary package doc comments and usage banners (README.md §5 is a copy-pasteable subset; `docs/deployment.md:50` a partial list). The design updates banners (§6 step 5, FM-1) but omits the package docs:

1. **`cmd/sso-ctl/auditverify/main.go` — package doc (lines 5–17).** Currently: *"reads audit events (from a JSON file or the live /api/v1/audit/events API)"*, *"Either source is mutually exclusive"*, two usage examples. Must become a three-source contract with a `--dsn` example line. The exit-code doc at line 36 ("0 clean / 1 break / 2 misuse") stays accurate — the new misuse rows (FM-1 → 2) and schema-mismatch (FM-4 → 1) fit it — but the source list goes stale without the edit.
2. **`cmd/sso-ctl/auditexport/main.go` — package doc (lines 10–18).** Currently: `--dsn <sqlite-dsn>` example and *"the store is opened via `auditsqlite.OpenReadOnly`, which NEVER migrates"* (line 17). After the change the opener is the shared `OpenStoreReadOnly` → postgres branch → `OpenAuditReadOnly`; the "never-migrates" wording must be preserved but the concrete opener named in the doc becomes factually wrong. The design's `--dsn` help-text replacement (§3.1) does not cover this doc.
3. **`cmd/sso-ctl/auditexport/agg.go` (new) — `RunAgg` doc comment.** Repo convention (both `auditverify` and `auditexport` package docs) is to state the exit-code contract; `RunAgg` needs "0 clean incl. empty window / 1 operational error / 2 misuse" plus the PROPOSED-flag statement.
4. **`cmd/sso-ctl/main.go` — top-level usage banner (lines 93–95).** The `Commands:` list gains an `audit-agg` line. Design §6 step 4 says "dispatch entry" and step 5 "usage banners" — make the main banner explicit.

## B. The four `docs/` contract files — checked verdicts

5. **`docs/config-reference.md` — no change required; design correct.** No new config keys; the only sso-ctl rows there (lines 449–450) are config-validation-scoped (`config schema` / `validate-schema`), not a general CLI reference. The audit CLI surface is documented per A1–A4. The design should record this rationale rather than a bare assertion.
6. **`docs/error-codes.md` — no change required, conditional.** No new wire codes and no new SDK sentinels: `OpenAuditReadOnly` returns descriptive found-vs-expected errors, and CLI exit codes are out of error-codes.md's documented scope (its own header: HTTP wire codes; the SDK sentinel tables at 416–432). **Condition:** the handoff must pin "no exported `Err*` sentinel" — an exported sentinel would trip `TestSentinelErrorsDocumented` (docscheck) and demand a table entry. The design's flat "no changes" omits this condition.
7. **`docs/feature-matrix.md` — no change required; design omits the statement.** Evidence: the matrix's generated registry (34 capabilities from `ops/build/capabilities.json`) has no CLI dimension — every row is an SDK/stock-binary/standalone-binary service surface with endpoints; sso-ctl appears nowhere in the file. The server-side postgres audit backend is *already* documented at config-reference.md:130 (`audit.backend`: memory · sqlite · postgres). A "postgres audit read-only" row would require inventing a new matrix taxonomy — out of scope for a CLI toolbelt change.
8. **`docs/openapi.yaml` — no change; correct (CLI-only, no endpoints).**

## C. Omissions to flag

- **O1 (real gap):** the package-doc updates in A1–A3. After implementation, two package docs are factually stale — the auditverify source list ("a JSON file or the live API") and auditexport's "opened via `auditsqlite.OpenReadOnly`". The design's §4.4 "frozen files" list doesn't include these docs, and §6's "usage banners" step doesn't name them.
- **O2 (conditional gap):** error-codes.md's exclusion is only valid if the implementation introduces no exported sentinel; the handoff doesn't say so.
- **O3 (coverage gap):** feature-matrix.md and observability.md are never mentioned. Both no-change verdicts are correct — observability.md:76 ("verify via `sso-ctl audit-verify`") stays accurate and the design adds no event types or metrics (audit-agg emits none; `token_issued` is pre-existing) — but the design should record them with evidence, since the task-level expectation was that at least one would change.
- **O4 (optional, not gate-bound):** README.md §5 — a `--dsn` audit-verify example line would fit the "copy-pasteable snippet" surface; the "All are offline except `audit-verify --from-url`" sentence remains true (`--dsn` is offline) and should be kept. `docs/deployment.md:50`'s toolbelt list is already partial (missing `audit-export`, `clients`, `sessions`) — adding `audit-agg` there is optional, not a contract.

**Bottom line:** the required update set is A1–A4 (all in `cmd/sso-ctl` package docs/banners, not `docs/`), B5–B8 stay untouched with three recorded conditions (no sentinel, matrix has no CLI dimension, no new events/metrics), and O1 is the one genuine omission the design must add to §6 step 5.

Review delivered to `docs/architect-analysis/auto/runs/enforce-application-x-www-form-urlencoded-on-cre-8a92c453/artifacts/adversarial_review-9c87f3a7/task-2-adversarial-review.md`. All claims verified read-only against the tree. Findings:

**(a) Log flood — real, with a blocking design gap**
- **A1 (must fix)**: `oauthwire.BindParamsFormOnly(ctx, v)` cannot emit the log as designed — `core.HandlerContext` (`shared/core/router.go:17-34`) has no logger and `oauthwire` is a leaf. Emission must move to the 8 call sites via the loggers they already hold (`s.logger` / `d.SrvLogger()`), with an exported bool predicate (`IsFormOnlyRejection`) and 3-value class enum; sentinel stays unexported.
- **A2 (medium, must bound)**: 7/8 sites are unauthenticated pre-parse, strict is the default, `WithRateLimit` is default-off (`options_httpstack.go:85-101`), and today bind failures log nothing — so this is the first per-request log at a credential surface. Reject-before-body-read makes the attack a header-only packet per line; ~10-15 MB/s at 100k RPS, plus synchronous logger mutex contention self-DoSing the endpoints. Proposed: per-class token bucket (≤2 lines/sec/class, global ≤8/sec) + optional exact counter metric via `s.Metrics()` (`accessors.go:128`), plus a testable acceptance case for bounded emission.

**(b) Log hygiene — safe as specified.** Enum-only class values; no body is ever read on the sentinel path. Pinned hard rules: never raw Content-Type (log injection via newlines), never request fields, endpoint only from a compile-time table, trace-id via ContextLogger explicitly accepted, never audit, predicate returns bool only.

**(c) Oracle — verified sound on all 8 sites.** Each site collapses to its pre-existing byte-identical body (traced `errorBody` on sso sites, plain `core.ErrorBody` on protocol sites, `authzErrorBody(ErrMFAInvalid)` on `/auth/mfa`). Strict mode *removes* two channels: the missing-CT-JSON-binds body-content oracle and the JSON-path bcrypt timing oracle (JSON never reaches `authenticateTokenClient`). `/auth/mfa` bind failures never reach `recordMFAFailure` (`:273,287,302` only) — no wire or audit distinction. Basic-over-body invariant preserved on the form path; JSON+Basic now collapses to uniform 400 (strictly better). `/device/verify`'s bearer-first order keeps auth outcomes CT-independent.

**(d) 400 invalid_request — correct.** RFC 6749 §3.2 mandates the CT; §5.2's "otherwise malformed" covers it; 415 has no OAuth error mapping and breaks the §5.1 envelope clients parse. Keep the bare envelope (no `error_description` — avoids a new body variant), all-or-nothing across classes, and note the 415-rejected rationale in `docs/error-codes.md`.

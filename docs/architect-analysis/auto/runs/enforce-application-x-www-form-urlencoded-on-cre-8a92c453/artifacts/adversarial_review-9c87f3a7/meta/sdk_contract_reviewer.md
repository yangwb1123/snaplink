All line numbers verified. Validation complete — report delivered to `docs/architect-analysis/auto/runs/enforce-application-x-www-form-urlencoded-on-cre-8a92c453/artifacts/adversarial_review-9c87f3a7/meta/openapi_gensdk_flip_reviewer.md`.

## Validation summary

**1. OpenAPI — VERIFIED (baseline exact, flip list complete/contained).** Structure-parser over `docs/openapi.yaml` proves exactly **8 paths** declare both form+JSON: `/token`, `/token/introspect`, `/token/revoke`, `/par`, `/device/code`, `/device/verify`, `/auth/mfa`, `/backchannel-authentication` (CIBA's actual name — operationId `postBackchannelAuthentication`, not `/ciba`). This maps 1:1 to the billing design's 8-site table; all 8 Go bind sites confirmed at the exact cited lines. No 9th dual path exists (90 JSON-only paths — login/register/admin/selfservice — untouched); all requestBodies are inline, each credential schema referenced exactly twice (its own path only), so the drop is fully contained. Pre-flip tree state is correct for design stage; the drop is pinned in billing step 7 and audit-provisioner C9.

**2. gensdk emission — MISMATCH (the one blocking finding).** Committed clients emit JSON for 7 ops (verified at client.ts:2823–2913 / client.py:2216–2288, zero `x-www-form-urlencoded` anywhere); **`postCIBA` does not exist** — absent from `sdk-surface.json` (316 ops), both clients, and even the `CIBABackchannelRequest` type. The sibling pins `usesFormBody` as a hardcoded **4-op** predicate that explicitly excludes `postDeviceCode`/`postDeviceVerify`/`postMFAComplete` "whose handlers still accept JSON" — a premise false against the accepted all-8 flip ("Switch the **eight** sites" — audit-provisioner step 3). Post-flip, generated device/MFA flows 400. Also re-confirmed the E7 drift: `/token` does declare JSON at :1139-1143 (the sibling's "only postToken is form-only" is wrong; the billing design's "all eight declare both" is accurate).

**3. `setFormField` json.RawMessage — pinned in the design set, absent from the billing design.** Pinned in the gensdk sibling (F-A, §6 step 8a with the exact `reflect.TypeOf(json.RawMessage(nil))` branch, T-9(g) "fails today") and the audit-provisioner sibling (F11, step 1). The billing design's step 8 references only "form emission + regenerate (R5.4)" — no F-A row in its failure-mode table. Cross-reference gap: an executor of the billing design alone ships form-PAR with `authorization_details`/`claims` silently binding nil.

**4. Sequencing — CONFIRMED.** Billing step 8 "ordered before/with step 3" + §3.3 + §3.4 row + requirements :434-435; sibling §4 "MUST land before the server form-only flip (T-9(d))" + F8 + §6 steps 8-12. Chain consistent: `setFormField` (sibling 8a) → regenerated clients (billing 8) → 8-site flip (billing 3), with T-9(d) as the fail-fast shared gate.

**Bottom line:** OpenAPI side and sequencing are exact; the prerequisite is pinned in the campaign's design set; the gensdk 4-op predicate vs 8-site flip (plus absent postCIBA) must be reconciled before implementation — either extend `usesFormBody` + its whole-output count pins to 7 ops, or explicitly re-decide the device/MFA generated flows break post-flip.

Re-validation complete — report at `docs/architect-analysis/auto/runs/enforce-application-x-www-form-urlencoded-on-cre-8a92c453/artifacts/adversarial_review-9c87f3a7/meta/migration_revalidation.md`. Every corrected-inventory claim was independently re-verified against the tree (HEAD `8948632c` + worktree).

## Verdict: the 9-step plan survives with four corrections

**1. Gates hold per-step with the new counts.** Each step's gate is build/vet/architecture (green by construction — steps 1-3 are compile-level; the 48+92+3 runtime breaks land only after step 3's flip and are absorbed by steps 4-5). `make ci` green at step 9, with two additions: a post-sweep counting gate (zero JSON posts to the 8 endpoints across the three surfaces) and the WIP caveat below.

**2. Corrections required:**
- **Step 2: "six fakes" → four** (`introspectDeps`, `parDeps`, `revokeDeps`, `cibaDeps` — verified `var _ XxxDeps` assertions; `introspect_geo_test.go`/`introspect_cache_test.go` reuse `introspectDeps`, so one edit covers three files). Fake update must be the same change as the interface-member addition or `protocols/oauth` won't compile.
- **Step 1 gains a `json.RawMessage` case in `setFormField`** — new blocking finding: the design's "RawMessage/claims branch untouched" claim is false; `setFormField` handles only String/Bool/Int/Pointer/Slice-String while `PARRequest.AuthorizationDetails`/`Claims` are `json.RawMessage` (handle_par.go:100/:105). Without it, the 3 protocol sites + `rar_test.go` + `claims_param_test.go` silently bind nil on form conversion and the OIDC claims-survival invariant breaks.
- **Step 5's inventory replaced wholesale** (19/48 + 24/92 + 3 = 143 posts; the 7 missed `test/` files verified). Pinned mechanic: the `interfaces/sso` sweep is **per-call-site** — the shared `rcovDo` (rootcov_flow_test.go:154) also serves `/auth/login` and admin JSON in the same files, so a blanket helper rewrite breaks them. `region_residency_mfa_test.go:143`'s 403 and `introspect_batch_signed`'s `Accept` header must survive conversion; the 2 nil-body CIBA probes stay untouched.
- **Step 8 executes before step 3** in wall-clock despite its number, covers all 8 sites (`postMFAComplete`/`postDeviceCode`/`postDeviceVerify` are committed-JSON today — the 4-op vs 8-site mismatch), and adopts F-A.

**3. WIP preservation (AGENTS.md §5-6).** The migration never touches `cmd/sso-ctl/apiclient/**`: no staging/committing/stashing/reset; commits are path-scoped; the package has zero snaplink imports (pure stdlib — verified), so build/vet/race gates cover it automatically with no compile coupling. Two disclosed conditions: (a) `make ci`'s `fmt` gate is red in this worktree **today** because `gofmt -l .` flags the untracked `check_test.go` — pre-existing, reported separately, absent from the committed tree; (b) the four legs (token.go:71/:236/:282/:334) 400 against strict at runtime — disclosed as a shipped-consumer breakage for the WIP owner to convert before landing, not fixed by the migration.

**4. Ordering confirmed.** R7.1 any time; R7.2 after step 3 (strict live), before step 9, no sweep dependency (it posts form itself) — the step-6 slot is correct. Four-fake update in step 2, before the protocol sites go strict.

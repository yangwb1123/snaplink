global
    log stdout format raw local0
    maxconn 20000

defaults
    log     global
    mode    http
    option  httplog
    timeout connect 5s
    timeout client  60s
    timeout server  60s

# --- App tier: L7 round-robin, drained on /readyz, XFF re-set (security gate) ---
frontend sso_http
    bind *:80
    bind *:443
    # Strip any inbound forwarded headers, then set our own from the real peer.
    # This is the trusted-edge contract sso-server relies on (AGENTS.md section 3).
    http-request del-header X-Forwarded-For
    http-request del-header X-Forwarded-Proto
    http-request set-header X-Forwarded-For %[src]
    http-request set-header X-Forwarded-Proto https
    default_backend sso_servers

backend sso_servers
    balance roundrobin
    option httpchk
    http-check send meth GET uri /readyz
    http-check expect status 200
    server sso1 sso1:8080 check inter 3s fall 3 rise 2
    server sso2 sso2:8080 check inter 3s fall 3 rise 2
    server sso3 sso3:8080 check inter 3s fall 3 rise 2

# --- Postgres: route :5432 to the Patroni LEADER via its REST health check ---
listen pg_leader
    bind *:5432
    mode tcp
    option httpchk GET /leader
    http-check expect status 200
    default-server inter 3s fall 3 rise 2 on-marked-down shutdown-sessions
    server pg1 pg1:5432 check port 8008
    server pg2 pg2:5432 check port 8008
    server pg3 pg3:5432 check port 8008

# --- Postgres: route :5433 to replicas (optional read offload, off by default) ---
listen pg_replicas
    bind *:5433
    mode tcp
    option httpchk GET /replica
    http-check expect status 200
    balance roundrobin
    default-server inter 3s fall 3 rise 2
    server pg1 pg1:5432 check port 8008
    server pg2 pg2:5432 check port 8008
    server pg3 pg3:5432 check port 8008

frontend stats
    bind *:8404
    stats enable
    stats uri /
    stats refresh 5s

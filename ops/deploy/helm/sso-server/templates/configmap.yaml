apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sso-server.fullname" . }}-config
  namespace: {{ include "sso-server.namespace" . }}
  labels:
    {{- include "sso-server.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}

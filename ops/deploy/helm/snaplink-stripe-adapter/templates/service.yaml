apiVersion: v1
kind: Service
metadata:
  name: {{ include "snaplink-stripe-adapter.fullname" . }}
  labels:
    {{- include "snaplink-stripe-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "snaplink-stripe-adapter.selectorLabels" . | nindent 4 }}
  ports:
    - name: https
      port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP

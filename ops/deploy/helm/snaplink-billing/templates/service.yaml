apiVersion: v1
kind: Service
metadata:
  name: {{ include "snaplink-billing.fullname" . }}
  labels:
    {{- include "snaplink-billing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "snaplink-billing.selectorLabels" . | nindent 4 }}
  ports:
    - name: https
      port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: snaplink-billing
  labels:
    app.kubernetes.io/name: snaplink-billing
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: snaplink-billing
  ports:
    - name: https
      port: 443
      targetPort: https
      protocol: TCP

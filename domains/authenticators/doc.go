// Package authenticators provides ready-to-use sso.Authenticator
// implementations: password, phone (SMS code), email (verification code),
// temporary token, key pair (Ed25519), API key (key/value), and X.509
// certificate.
package authenticators
